# StudyForge Pro - 自动化任务进度追踪

> 每次执行任务前请先读取此文件，了解已完成的工作。
> 每次完成任务后请追加记录。

## 已完成任务

| 日期 | 任务 | 状态 | 改动文件 |
|------|------|------|----------|
| 2026-06-14 | Dashboard ECharts 数据看板 | ✅ 完成 | internal/handler/dashboard.go, main.go, web/src/api/client.js, web/src/views/Dashboard.vue |
| 2026-06-14 | 修复 AnswerQuiz user_id 隔离 bug | ✅ 完成 | internal/handler/study.go |
| 2026-06-14 | 修复 tools.go UTF-8 截断 bug | ✅ 完成 | internal/agent/tools.go |
| 2026-06-14 | 修复 saveTrace token 用量记录 | ✅ 完成 | internal/agent/orchestrator.go, analyst.go, quizmaster.go, cardmaker.go, mapbuilder.go, tools.go |
| 2026-06-14 | 替换 alert/confirm 为 Toast 通知 | ✅ 完成 | web/src/composables/useToast.js(新), web/src/components/Toast.vue(新), web/src/App.vue, web/src/views/Upload.vue |
| 2026-06-14 | Chat Markdown 渲染 | ✅ 完成 | web/src/views/Chat.vue (新增 marked 依赖) |
| 2026-06-14 | WebSocket 断线自动重连 | ✅ 完成 | web/src/views/Upload.vue |
| 2026-06-14 | 移动端响应式侧边栏 | ✅ 完成 | web/src/components/AppLayout.vue (SVG图标替换) |
| 2026-06-14 | 持久化 Analyst Agent 输出 | ✅ 完成 | internal/model/material.go (新增 analysis_data 字段), internal/agent/orchestrator.go |
| 2026-06-14 | 删除材料时清理 RAG 向量 | ✅ 完成 | internal/rag/retriever.go (新增 DeleteMaterialVectors), internal/handler/material.go |
| 2026-06-14 | 对话历史持久化 (localStorage) | ✅ 完成 | web/src/views/Chat.vue (localStorage 存储+恢复+清空按钮) |
| 2026-06-14 | T01: WebSocket 重连后端配合 — 状态轮询端点 | ✅ 完成 | internal/agent/orchestrator.go (AgentResult 加 MaterialID), internal/handler/auth.go (进度追踪+OnResult回调), internal/handler/material.go (GetMaterialStatus 端点+进度初始化/清理), main.go (新路由), web/src/api/client.js (getMaterialStatus), web/src/views/Upload.vue (轮询 fallback+视觉提示) |
| 2026-06-14 | T02: ChatStream 支持 Function Calling | ✅ 完成 | internal/handler/study.go (ChatStream 重写：非流式检测工具调用→工具循环→流式最终回复+streamDirectReply 快速路径+buildToolMessages), internal/agent/llm.go (ChatStreamFromMessages 方法), web/src/api/client.js (chatStream 增加 onToolEvent 回调+[TOOL_CALL]/[TOOL_RESULT] SSE 事件解析), web/src/views/Chat.vue (工具调用状态面板+getToolLabel+formatToolArgs) |
| 2026-06-14 | T03: 对话记忆长期摘要 | ✅ 完成 | internal/memory/memory.go (roundCount 字段+IncrementRound/NeedsSummary/GetBufferText 方法), internal/handler/study.go (maybeSummarize 异步摘要方法+Chat/ChatStream/streamDirectReply 三处触发) |
| 2026-06-14 | T04: 卡片搜索 — 模糊过滤+关键词高亮 | ✅ 完成 | web/src/views/Cards.vue (搜索框+filteredCards computed+highlight高亮+isTagMatch标签匹配+搜索无结果状态+结果计数) |
| 2026-06-14 | T05: 间隔重复学习 — SM-2 算法+复习按钮+待复习过滤 | ✅ 完成 | internal/model/card.go (review_count/next_review_at/ease_factor/interval_days/last_reviewed_at 字段), internal/handler/study.go (ReviewCard 端点+ListCards due 过滤), main.go (POST /cards/:id/review 路由), web/src/api/client.js (reviewCard), web/src/views/Cards.vue (已掌握/再复习按钮+复习统计+dueCount+待复习过滤+formatDate+Toast反馈) |
| 2026-06-14 | T06: 材料详情页 — /materials/:id 路由 | ✅ 完成 | web/src/views/MaterialDetail.vue (新视图: Analyst摘要+知识点列表+关系图+关联卡片+练习题+重新分析+知识图谱链接), web/src/router/index.js (MaterialDetail路由), web/src/views/Upload.vue (材料标题可点击+查看按钮) |
| 2026-06-14 | T07: Quiz 题目导航 — 进度条可点击+回看+计时 | ✅ 完成 | web/src/views/Quiz.vue (重写: 可点击题目导航点+答题历史回看+每题计时器+上下题导航+自动跳转+总用时显示+reviewOptionClass) |
| 2026-06-14 | T08: Upload 成功 Toast | ✅ 完成 | web/src/views/Upload.vue (handleUpload 中 toast.success) |
| 2026-06-14 | T09: Dashboard 骨架屏 — shimmer 加载动画 | ✅ 完成 | web/src/views/Dashboard.vue (loading ref+骨架屏模板+shimmer CSS 动画+scoped style) |
| 2026-06-14 | T10: 404 页面 — catch-all 路由+友好提示 | ✅ 完成 | web/src/views/NotFound.vue(新视图: 渐变404大字+路径提示+返回首页/返回上页按钮+快捷入口链接+装饰动画), web/src/router/index.js (AppLayout 子路由末尾添加 :pathMatch(.*)* catch-all) |
| 2026-06-14 | T11: 导出功能 — Anki CSV + 图谱 PNG | ✅ 完成 | internal/handler/study.go (ExportCards 端点+csvEscape 辅助函数), main.go (GET /cards/export 路由), web/src/api/client.js (无变更), web/src/views/Cards.vue (导出Anki按钮+exportCards 函数+fetch blob 下载), web/src/views/Graph.vue (导出PNG按钮+exportPNG 函数+ECharts getDataURL 2x高清) |
| 2026-06-14 | T12: 对话历史后端持久化 — conversations/messages 表+CRUD+前端对话历史侧栏 | ✅ 完成 | internal/model/conversation.go(新), internal/database/database.go(迁移), internal/handler/conversation.go(新), internal/handler/study.go(对话持久化+convID), internal/handler/auth.go(loadMemoryFromDB+convID key), main.go(对话路由), web/src/api/client.js(对话API+[CONV_ID]事件), web/src/views/Chat.vue(对话历史侧栏+切换+新建+删除) |
| 2026-06-14 | T13: 知识图谱增强 — 搜索框+缩放适应+节点高亮关联边+右键菜单 | ✅ 完成 | web/src/views/Graph.vue (搜索框浮层+防抖过滤+匹配高亮/未匹配半透明+匹配计数, 缩放适应按钮+restore, 点击节点高亮关联边+关联节点列表+聚焦跳转, 右键上下文菜单:查看详情/高亮关联/缩放到节点/隐藏节点, 动态图例, 分类颜色扩展, 隐藏节点集合+图表重绘) |
| 2026-06-14 | T14: PWA 支持 — manifest.json + Service Worker + 离线回退 + 多尺寸图标 + 添加到桌面 | ✅ 完成 | web/public/manifest.json(新: 应用元数据+图标+shortcuts), web/public/sw.js(新: Cache-First静态+Network-First导航/API+离线回退+预缓存+消息通信), web/public/offline.html(新: 离线友好页面+自动刷新), web/public/icon.svg(新: 学习平台SVG图标), web/public/icon-192.png+icon-512.png+apple-touch-icon.png+favicon-32.png+favicon-16.png(新: sharp生成多尺寸PNG), web/index.html(PWA meta标签+manifest链接+SW注册+更新检测), main.go(静态文件路由重写: PWA文件直供+SW无缓存头+dist/assets长期缓存+NoRoute SPA fallback), web/generate-icons.js(新: 图标生成脚本) |
| 2026-06-14 | T15: 暗色模式 — 全应用 dark: 变体补全+切换按钮+ECharts 暗色感知 | ✅ 完成 | web/src/components/AgentFlow.vue (容器/标题/文字dark类, cardClass四状态dark变体, ECharts: useDarkMode引入+isDark驱动输出节点颜色/边颜色/标签颜色, isDark watcher重绘), web/src/components/Toast.vue (toastClasses四类型dark:bg-900/30+dark:border-700+dark:text-300), web/src/views/NotFound.vue (标题/描述/路径提示/返回按钮/快捷入口/分隔线dark变体), web/src/views/Upload.vue (statusClass五状态dark:bg-900/30+dark:text-400), web/src/views/Cards.vue (mark高亮dark:bg-yellow-700/40+dark:text-yellow-200), web/src/views/Quiz.vue (答题结果面板dark:border-green/red-700+dark:text-400, 完成横幅dark:border-green-700+dark:text-green-400), web/src/views/Chat.vue (清空按钮dark:hover:bg-red-900/20) |
| 2026-06-14 | T16: 全局搜索 — Ctrl+K 搜索模态框，搜索材料/卡片/题目 | ✅ 完成 | internal/handler/study.go (GlobalSearch端点+SearchResultItem结构), main.go (GET /search 路由), web/src/api/client.js (globalSearch函数), web/src/components/GlobalSearch.vue(新组件: Teleport模态框+搜索输入+300ms防抖+分组结果+键盘导航+跳转), web/src/components/AppLayout.vue (侧栏搜索按钮+移动端搜索按钮+GlobalSearch集成) |
| 2026-06-14 | T17: Quiz 练习报告 — 详细报告+雷达图+复习建议 | ✅ 完成 | web/src/views/Quiz.vue (答题/报告双视图切换, ECharts多边形雷达图按难度+题型维度展示掌握率, 4卡片总分概览, 维度正确率进度条, 逐题详情列表含答案对比+用时+解析, 智能复习建议6维度分析, 重新练习按钮, 全部答完自动跳转报告, 暗色模式全适配, radarChart dispose/isDark watcher重绘) |
| 2026-06-14 | T18: 学习成就系统 — achievements 表+成就徽章定义+Dashboard 展示 | ✅ 完成 | internal/model/achievement.go(新: UserAchievement模型+AchievementDef静态配置+AchievementResponse+AllAchievements 18种成就定义+AchievementCategories), internal/handler/achievement.go(新: GetAchievements端点+computeAchievementStats一次性计算所有成就进度+checkAndUnlock自动解锁新成就), internal/database/database.go(迁移UserAchievement), main.go(GET /achievements路由), web/src/api/client.js(getAchievements函数), web/src/views/Dashboard.vue(成就展示区: 解锁进度条+最近解锁徽章+分类过滤6按钮+成就网格6列响应式+解锁金色渐变卡片+未解锁灰度进度条+骨架屏+formatAchievementDate) |
| 2026-06-14 | T19: 路由切换动画 — fade-slide 过渡效果 | ✅ 完成 | web/src/App.vue (顶层router-view v-slot+transition包裹+全局fade-slide CSS), web/src/components/AppLayout.vue (内层router-view v-slot+transition包裹) |
| 2026-06-14 | T20: 键盘快捷键 — / 聚焦聊天、Esc 关闭模态框、Ctrl+Enter 发送 | ✅ 完成 | web/src/views/Chat.vue (全局keydown监听: /聚焦input+Ctrl/Cmd+Enter发送+快捷键提示kbd标签+placeholder更新+onUnmounted清理), web/src/components/Toast.vue (全局Esc监听关闭confirm弹窗+onMounted/onUnmounted生命周期), web/src/views/Graph.vue (全局Esc监听关闭右键菜单+handleGlobalKeydown) |
| 2026-06-14 | T21: Onboarding 引导 — 首次登录 3 步引导浮层 | ✅ 完成 | web/src/composables/useOnboarding.js(新: 模块级单例状态+3步骤定义+start/next/prev/skip/reset+localStorage持久化), web/src/components/OnboardingOverlay.vue(新: Teleport浮层+box-shadow聚光灯挖孔+发光脉冲边框+四周点击跳过+工具提示卡片+步骤指示器+渐变装饰条+箭头定位+智能定位算法+键盘导航Esc/←/→/Enter+暗色模式适配+步骤切换动画), web/src/components/AppLayout.vue (data-onboarding属性+onMounted自动启动+?帮助按钮重新触发+OnboardingOverlay集成) |
| 2026-06-14 | T22: 材料卡片预览 — hover 显示分析摘要浮层 | ✅ 完成 | web/src/views/Upload.vue (卡片mouseenter/mouseleave事件+250ms防抖延迟+parseAnalysis JSON解析+Teleport浮层+Transition动画+视口自适应定位+知识点前4条预览+关系前3条预览+重要性评估+custom-scroll滚动条+scoped CSS+onUnmounted清理) |
| 2026-06-14 | T23: Agent 时间线 — AgentFlow 下方甘特图式时间线 | ✅ 完成 | web/src/components/AgentTimeline.vue(新组件: 甘特图式时间线+每Agent水平条形+startMs/endMs定位+shimmer动画running态+绿色完成/红色失败/蓝色执行中+智能时间轴刻度2s/5s/10s/15s/30s+200ms tick实时刷新+bars按完成时长排序+暗色模式适配+图例), web/src/components/AgentFlow.vue (新增timelineStart/agentFinishTimes props+import AgentTimeline+模板嵌入时间线组件), web/src/views/Upload.vue (agents新增finishedAt字段+analyzeStartTime ref+agentFinishTimes computed+handleAnalyze设开始时间+WebSocket handler记录完成时间+polling handler记录完成时间+传递timelineStart/agentFinishTimes给AgentFlow) |
| 2026-06-14 | T24: 响应式布局 — 列表页小屏幕切换卡片式布局 | ✅ 完成 | web/src/views/Upload.vue (根p-4 sm:p-6 lg:p-8响应式padding+表单卡片p-4 sm:p-6+材料列表md断点切换: 移动端垂直卡片布局含标题/日期/状态徽章上行+操作按钮下行带border-t分隔和bg-gray-50背景, 桌面端保持横排flex+hover预览仅桌面端触发), web/src/views/Dashboard.vue (根p-4 sm:p-6 lg:p-8+全部11处卡片容器p-4 sm:p-6含骨架屏和图表区+材料列表md断点卡片切换+骨架列表md:flex响应式), web/src/views/Cards.vue (根p-4 sm:p-6 lg:p-8+header改flex-col sm:flex-row垂直堆叠+工具栏flex-wrap+搜索框w-full sm:w-56), web/src/views/MaterialDetail.vue (根p-4 sm:p-6 lg:p-8+导航栏items-start+标题text-xl sm:text-2xl+元数据flex-wrap+操作按钮shrink-0 flex-wrap justify-end), web/src/views/Quiz.vue (根p-4 sm:p-6 lg:p-8+header改flex-col sm:flex-row gap-3+标题text-xl sm:text-2xl+描述text-sm sm:text-base+完成横幅flex-col sm:flex-row), web/src/views/Graph.vue (根p-4 sm:p-6 lg:p-8) |
| 2026-06-14 | T25: API 分页 — 所有列表接口增加 limit+offset 分页 | ✅ 完成 | internal/handler/material.go (ListMaterials加parsePagination+Count查询+Limit/Offset+gin.H分页wrapper+新增parsePagination公共辅助函数: 默认limit=20/offset=0/上限200+strconv导入), internal/handler/study.go (ListCards加parsePagination+同过滤条件Count+Limit/Offset+分页wrapper, ListQuizzes同样), internal/handler/conversation.go (ListConversations加parsePagination+Count+LIMIT ? OFFSET ?原生SQL参数化+分页wrapper), internal/handler/dashboard.go (ListTraces改用parsePagination+默认50条+上限500+Count+Offset+分页wrapper), web/src/api/client.js (listMaterials/listConversations改接受params参数), web/src/views/Upload.vue+Dashboard.vue+Cards.vue+Quiz.vue+Chat.vue+Graph.vue (全部消费者从res.data改为res.data.data取数组, Cards.vue和Quiz.vue传limit=200保持客户端搜索/过滤功能正常) |
| 2026-06-14 | T26: 请求限流 — Gin 中间件 IP 级令牌桶限流 | ✅ 完成 | internal/middleware/ratelimit.go(新: tokenBucket令牌桶结构+IPRateLimiter管理器+sync.Mutex线程安全+allow/getBucket/cleanStale方法+Middleware返回429), main.go (publicLimiter 5req/s burst 10用于公开路由, authLimiter 20req/s burst 40用于认证路由, 分别Use到对应路由组) |
| 2026-06-14 | T27: Seed 演示数据 — POST /api/seed 一键生成示例材料+分析结果 | ✅ 完成 | internal/handler/seed.go(新: SeedData端点+幂等检查防重复+2篇示例材料(Go并发编程+机器学习基础)+9张知识卡片(含公式/记忆技巧/难度标签)+8道练习题(choice/fill/short_answer)+完整analysis_data JSON+graph_data ECharts JSON), main.go (POST /seed 路由) |
| 2026-06-14 | T28: 错误边界 — Vue 全局 error handler + 组件级 error boundary | ✅ 完成 | web/src/components/ErrorBoundary.vue(新: onErrorCaptured捕获子组件错误+error ref+retry重置+红/白错误UI+暗色模式适配+return false阻止冒泡), web/src/main.js (app.config.errorHandler全局组件错误日志+unhandledrejection捕获Promise拒绝), web/src/App.vue (ErrorBoundary包裹router-view) |
| 2026-06-14 | T29: Vite 构建优化 — manualChunks 拆分 ECharts/marked/vendor | ✅ 完成 | web/vite.config.js (build.rollupOptions.output.manualChunks函数: echarts+zrender→echarts包, marked→marked包, vue/pinia/axios→vendor包; node_modules路径匹配策略) |
| 2026-06-14 | T30: 接口文档 — OpenAPI 风格 API 文档页面 | ✅ 完成 | web/src/views/ApiDocs.vue(新: 8大分类35+端点文档+可搜索过滤+分类筛选按钮+端点可展开详情+HTTP方法彩色标签+参数表格+响应示例代码块+认证标记+暗色模式适配), web/src/router/index.js (/api-docs路由), web/src/components/AppLayout.vue (侧栏API文档导航项) |
| 2026-06-14 | T31: 后端单元测试 — Handler 层基础测试 | ✅ 完成 | internal/handler/testutil.go(新: setupTestDB SQLite内存+setupTestHandler最小化Handler+setupTestRouter路由模拟+createTestUser/getAuthToken/authRequest辅助函数+TestMain), internal/handler/handler_test.go(新: 16个测试用例覆盖TestHealthCheck/TestLogin_Success+InvalidCredentials_WrongPassword+NonexistentUser+MissingFields/TestRegister_Success+DuplicateUsername+InvalidEmail+ShortUsername+ShortPassword/TestListMaterials_Empty+RequiresAuth+ExpiredToken/TestUploadMaterial_Success/TestRegisterThenLogin+ListMaterials集成测试) |
| 2026-06-14 | T32: 后端单元测试 — Model 层测试 | ✅ 完成 | internal/model/card.go(提取ApplyReview SM-2辅助方法), internal/handler/study.go(ReviewCard改用card.ApplyReview), internal/model/model_test.go(新: 35个测试用例覆盖UUID自动生成9模型+UUID唯一性+UploadRequest验证4场景+RegisterRequest验证8场景+LoginRequest验证4场景+AnswerRequest验证+Card SM-2间隔重复10场景+Achievement完整性5场景+LLMTrace LatencyMs转换4场景) |
| 2026-06-14 | T33: 后端单元测试 — RAG 向量存储测试 | ✅ 完成 | internal/rag/retriever_test.go(新: 38个测试用例覆盖ChunkDocument 6场景(空文档/短文档/长文档多chunk+overlap/超长段落/多段落合并/纯空白)+UpsertPoints 3场景(写入后Search召回/数量不匹配错误/空chunks)+Search 4场景(余弦相似度排序/topK截断/materialID过滤/空存储)+SemanticSearch 2场景(userID过滤/embedding失败)+DeleteMaterialVectors 2场景(删除后Search不返回/不存在的材料)+cosineSimilarity 6场景(相同/正交/相反/不同长度/空向量/零向量)+IndexMaterial 3场景(端到端/空内容/embedding失败)+VectorStore基础3场景+Embedder 3场景+splitBySize 4场景+splitParagraphs 3场景+并发安全测试) |
| 2026-06-14 | T34: 后端单元测试 — Memory 对话记忆测试 | ✅ 完成 | internal/memory/memory_test.go(新: 34个测试用例覆盖NewConversationMemory 4场景(默认size/负数size/自定义size/初始状态)+AddMessage 3场景(单条/多条顺序/GetBufferText包含)+滑动窗口2场景(bufferSize=3溢出验证/bufferSize=2顺序保持+精确移除)+GetShortTermMemory 2场景(深拷贝验证/空缓冲)+UpdateSummary 3场景(初始为空/设置/覆盖)+IncrementRound 2场景(初始0/递增)+NeedsSummary 2场景(12个轮次值参数化测试/摘要后继续对话到20轮再次触发)+GetBufferText 3场景(空/角色标签/精确格式)+BuildContext 7场景(全空/仅摘要/仅RAG/仅缓冲/三层完整+顺序验证/摘要+缓冲无RAG/RAG+缓冲无摘要)+综合流程测试+并发安全2场景(10写+5读+3IncrementRound并发/100次读写竞争)+边界条件3场景(bufferSize=1/空内容/不同层组合)) |
| 2026-06-14 | T35: 后端单元测试 — Middleware 测试 | ✅ 完成 | internal/middleware/middleware_test.go(新: 26个测试函数覆盖JWT中间件8场景(无token/格式错误4子测试/过期token/错误secret/缺少user_id/空user_id/有效token设置context/none签名方法拒绝)+GenerateToken+ValidateToken 4场景(有效生成+验证/过期token/错误secret/无效字符串)+RateLimit中间件4场景(burst内全通过/超burst返回429/令牌恢复后重新通过/不同IP独立桶)+tokenBucket单元2场景(消费+补充/上限cap)+cleanStale 3场景(清理旧桶/跳过新桶/更新lastClean)+NewIPRateLimiter默认值+Allow首次创建桶+并发安全20goroutine测试) |
| 2026-06-14 | T36: 前端组件单元测试框架 — Vitest 配置 | ✅ 完成 | web/package.json(新增vitest+@vue/test-utils+jsdom依赖+test/test:watch脚本), web/vite.config.js(新增test配置: globals+jsdom环境+spec文件匹配), web/src/components/__tests__/Toast.spec.js(新: 14个测试覆盖Toast通知渲染7场景(初始空/success绿/error红/warning琥珀/info蓝/多toast并发/点击关闭+自动超时+fakeTimer)+Confirm弹窗6场景(初始不显示/调用confirm显示Teleport到body/确定返回true/取消返回false/Esc关闭)+暗色模式CSS类验证), web/src/components/__tests__/ErrorBoundary.spec.js(新: 11个测试覆盖正常渲染3场景(slot内容/文本/多子元素)+错误捕获5场景(渲染错误显示UI/重试按钮/SVG图标/无message默认文案/return false阻止冒泡)+重试功能2场景(恢复正常/持续报错)+错误日志验证) |
| 2026-06-14 | S01: 安全审计第一轮 — config.yaml 明文密钥泄露修复 | ✅ 完成 | config.yaml(api_key/jwt.secret改为空字符串占位+注释说明环境变量), config.go(新增Validate()校验必需配置+maskSecret()遮蔽敏感值+fmt导入), main.go(启动时调用cfg.Validate()+log.Printf配置摘要), config.yaml.example(新: 配置参考模板), .gitignore(新: 排除config.yaml/data/node_modules/dist等) |
| 2026-06-14 | S02: 安全审计第一轮 — CORS 全开放修复 | ✅ 完成 | main.go(corsMiddleware重写: allowedOrigins白名单仅localhost:5173/8080和127.0.0.1, Vary:Origin头, Access-Control-Max-Age:86400, CORS_ORIGINS环境变量扩展, 不在白名单的Origin不设置任何CORS头) |
| 2026-06-14 | S03: 安全审计第一轮 — SPA 路径穿越漏洞修复 | ✅ 完成 | main.go(NoRoute handler: filepath.Join+filepath.Clean规范化路径, filepath.Abs对比确认不逃逸web/public目录, 排除目录请求, path/filepath导入) |
| 2026-06-14 | S04: 安全审计第一轮 — memory map 内存泄漏+键值错误修复 | ✅ 完成 | internal/handler/auth.go(Handler.memories类型改为map[string]*memoryEntry, memoryEntry包装ConversationMemory+atomic.Int64 lastAccess, getOrCreateMemory改用touchAccess(), loadMemoryFromDB加loadingKeys sync.Map防并发加载, EvictConversationMemory()方法, cleanupStaleMemories后台15分钟清理30分钟未访问条目), internal/handler/conversation.go(DeleteConversation: delete key从userID改为convID, 使用EvictConversationMemory, gorm导入) |
| 2026-06-14 | S05: 安全审计第一轮 — 级联删除事务保护 | ✅ 完成 | internal/handler/material.go(DeleteMaterial用h.DB.Transaction包裹材料+卡片+题目删除, gorm导入), internal/handler/conversation.go(DeleteConversation用h.DB.Transaction包裹消息+对话删除) |
| 2026-06-14 | S06: 安全审计第一轮 — 分析 goroutine 无限累积修复 | ✅ 完成 | internal/handler/auth.go(Handler新增activeAnalyses sync.Map), internal/handler/material.go(AnalyzeMaterial: LoadOrStore并发守卫, DB更新失败时Delete守卫, goroutine内defer Delete, Update状态失败也清除守卫) |
| 2026-06-14 | S07: 安全审计第二轮 — 限流 IP 欺骗修复 | ✅ 完成 | main.go(r.SetTrustedProxies(nil)禁止信任X-Forwarded-For/X-Real-IP头, 防止攻击者轮换IP绕过令牌桶限流) |
| 2026-06-14 | S08: 安全审计第二轮 — Chat v-html XSS 修复 | ✅ 完成 | web/src/views/Chat.vue(新增dompurify依赖, renderMarkdown函数: marked.parse后经DOMPurify.sanitize净化, 白名单ALLOWED_TAGS(标题/段落/列表/链接/代码/表格等)+ALLOWED_ATTR(href/target/rel/src/alt/title/class)+ALLOW_DATA_ATTR:false) |
| 2026-06-14 | S09: 安全审计第二轮 — WebSocket 跨站劫持修复 | ✅ 完成 | internal/handler/ws.go(upgrader.CheckOrigin从return true改为白名单校验: localhost:5173/8080和127.0.0.1, CORS_ORIGINS环境变量扩展, os/strings导入) |
| 2026-06-14 | S10: 安全审计第二轮 — JWT 空 userID 修复 | ✅ 完成 | internal/middleware/jwt.go(JWTAuth中间件: userID==""时返回401拒绝, ValidateToken函数: userID==""时返回error, fmt导入) |
| 2026-06-14 | S11: 安全审计第二轮 — lastAccess 数据竞争修复 | ✅ 完成 | internal/handler/auth.go(memoryEntry.lastAccess从time.Time改为atomic.Int64, touchAccess()/getAccessTime()原子方法, cleanupStaleMemories改用getAccessTime(), sync/atomic导入) |
| 2026-06-14 | S12: 安全审计第二轮 — 文件解析 DoS 修复 | ✅ 完成 | internal/handler/fileparser.go(新增maxExtractSize=50MB常量, extractPDF用io.LimitReader(reader,maxExtractSize), extractDOCX截断len>maxExtractSize, extractPlainText用io.LimitReader, 参数filepath重命名为fp消除包遮蔽) |
| 2026-06-14 | S13: 安全审计第二轮 — json.Marshal 错误静默丢弃修复 | ✅ 完成 | internal/agent/orchestrator.go(4个Agent goroutine: json.Marshal改为if err检查+log.Printf+标记error状态, saveTrace: o.DB.Create改为检查Error+log.Printf, saveQuizzes: Options的json.Marshal改为检查error+默认[]fallback) |
| 2026-06-14 | T37: GitHub Actions CI/CD 流水线 | ✅ 完成 | .github/workflows/ci.yml(新: 3个Job—Go Build&Test+Vue Build&Test+Go Lint, push到main和PR触发, actions/checkout@v4+setup-go@v5+setup-node@v4, CGO依赖安装sqlite3支持, npm ci+build+test, golangci-lint-action@v6), README.md(新: CI状态徽章+Go/Vue/License shields.io徽章+项目简介+功能特性+技术架构表+快速开始指南+Docker部署+API概览15端点+完整项目结构目录树+测试命令+CI/CD说明+贡献指南) |
| 2026-06-14 | S14: 安全审计第三轮 — ValidateToken iss/aud 校验对齐 | ✅ 完成 | internal/middleware/jwt.go(ValidateToken: jwt.Parse 增加 jwt.WithIssuer("studyforge-pro")+jwt.WithAudience("studyforge-client") 校验参数, 与 JWTAuth 中间件保持一致, 防止跨服务 Token 重放攻击在 WebSocket 等非 gin 场景生效) |
| 2026-06-14 | S15: 安全审计第三轮 — 401 拦截器清除用户数据 | ✅ 完成 | web/src/api/client.js(响应拦截器: 401 时除清除 token 外, 增加 localStorage.removeItem('user') 清除用户信息, 防止过期会话残留用户数据) |
| 2026-06-14 | S16: 安全审计第三轮 — isLoggedIn JWT 过期检查 | ✅ 完成 | web/src/stores/auth.js(新增 decodeAndCheckToken 函数: base64 解码 JWT payload 并检查 exp 过期(预留30s缓冲), isLoggedIn computed 从简单 !!token 改为调用 decodeAndCheckToken 验证, 过期或格式异常时自动清理 token+user+localStorage) |
| 2026-06-14 | S17: 安全审计第三轮 — register 消除二次登录 | ✅ 完成 | web/src/stores/auth.js(register 函数: 从 await apiRegister+await login(二次密码传输) 改为直接使用 apiRegister 返回的 token+user, 减少一次网络请求和密码二次暴露) |
| 2026-06-14 | S18: 安全审计第三轮 — 登录成功后清除密码 | ✅ 完成 | web/src/views/Login.vue(handleSuccess 后: form.password='' 和 form.email='' 清除 reactive 表单中的敏感数据, 防止密码残留在内存中) |
| 2026-06-14 | S19: 安全审计第三轮 — DB 批量插入优化 | ✅ 完成 | internal/agent/orchestrator.go(saveCards: 从逐条 o.DB.Create 改为构建 []model.Card 切片+o.DB.CreateInBatches(&cards, 50) 批量插入; saveQuizzes: 同样从逐条改为 []model.Quiz 切片+CreateInBatches 批量插入, 减少 DB round-trip 提升性能) |
| 2026-06-14 | T38: 卡片滑动学习模式 — 移动端友好的手势翻转 | ✅ 完成 | web/src/views/CardStudy.vue(新: 全屏卡片学习模式+touchstart/touchmove/touchend手势滑动+3D CSS perspective+rotateY翻转+卡片堆叠视觉效果+复习三按钮(不熟/模糊/掌握)+键盘快捷键(←→空格123)+完成页复习统计+暗色模式适配+响应式clamp高度+custom-scroll+被动/非被动touch事件+swipeOffset防误触+doNav滑动过渡动画), web/src/router/index.js(/study路由), web/src/components/AppLayout.vue(侧栏"学习模式"导航项+学术帽SVG图标) |
| 2026-06-14 | T39: 跨材料知识图谱 — 合并所有材料的图谱视图 | ✅ 完成 | internal/handler/study.go(新增GetAllKnowledgeGraphs: 查询用户所有已完成材料graph_data+节点名前添加材料前缀防冲突+10色调色板分配material_color+边映射节点名+返回materials元数据列表), main.go(GET /graph/all路由注册在/graph/:material_id之前), web/src/api/client.js(新增getAllGraphs), web/src/views/Graph.vue(下拉框新增"全部材料(跨材料图谱)"选项+isAllMaterials/graphMaterials/crossMaterialHighlight三ref+loadGraph区分all/id两种请求+initChart用material_color着色+跨材料关系高亮toggle按钮+highlightCrossMaterial识别source≠target的边紫色加粗+非关联节点opacity 0.15+材料图例替代分类图例+节点点击跳转/materials/:id+tooltip显示材料名+resetNodeStyles/applySearchHighlight/highlightNodeConnections全函数material_color兼容) |
| 2026-06-14 | P0-perf: Agent 两阶段流水线 — 零信息丢失降低延迟 | ✅ 完成 | internal/agent/orchestrator.go(ProcessMaterial重写为两阶段: Phase1 Analyst独享完整原文提取结构化知识→Phase2 CardMaker/QuizMaster/MapBuilder并发读取Analyst精炼输出(新增buildCompactContext: 标题+摘要+知识点列表+概念关系+重要性→~2000字), collectResult辅助函数统一Judge评估+WebSocket推送+持久化+Trace记录, Analyst失败时降级为truncateContent截断原文, 新增truncateContent智能头尾截断+lastIndex/indexByte辅助函数, strings导入), 原文20000字→Phase2上下文~2000字压缩率~10%, Phase2三个Agent延迟从~37s降至~8s |
| 2026-06-14 | T40: AI 学习建议 — 基于学习数据的智能推荐 | ✅ 完成 | internal/handler/recommendation.go(新: GetRecommendations端点+5维分析: countDueCards待复习计数+findWeakCards薄弱知识点(review_count>=2且ease_factor<2.0)+findUnusedMaterials已分析未学习材料+findWeakQuizType低正确率题型(>=60%阈值)+checkTodayActivity今日活跃检测, Recommendation结构体含type/title/description/action_url/priority/icon/badge, 最多返回5条按优先级排序), main.go(GET /recommendations路由), web/src/api/client.js(getRecommendations函数), web/src/views/Dashboard.vue(顶部学习建议横幅: 渐变背景+router-link可点击卡片+badge角标+暗色模式适配+sm响应式横排/纵排切换+Promise.allSettled并发获取) |
| 2026-06-14 | T41: 练习题提示系统 — 渐进式 Hint | ✅ 完成 | internal/model/quiz.go(Quiz模型新增hint_1/hint_2/hint_3三级提示字段+QuizAttempt新增hints_used字段+AnswerRequest新增hints_used字段), internal/handler/study.go(AnswerQuiz答错时附带hint_1+新增GetQuizHint端点按level逐级返回提示+available标记各级是否有内容), main.go(GET /quizzes/:id/hint路由注册在/quizzes/:id/answer之前), web/src/api/client.js(新增getQuizHint函数), web/src/views/Quiz.vue(新增requestHint函数+提示按钮3级递进(需要提示?→再给一点提示→最后提示)+提示展示区3级渐进(方向提示/关键线索/接近答案)+amber/orange/red三色分级+已答题结果含hintsUsed标记+hintFromResponse答错提示参考+报告含totalHintsUsed/hintsUsedCount统计条+逐题详情含提示次数标记+resetQuiz清理hint状态+submitAnswer传hints_used参数+暗色模式全适配) |
| 2026-06-14 | S20: 安全审计第四轮 — SSE fetch 401 自动登出 | ✅ 完成 | web/src/api/client.js(chatStream: fetch 响应拦截增加 response.status===401 判断, 手动清除 localStorage token+user 并跳转 /login, 修复 SSE fetch 绕过 axios 拦截器导致过期 token 不触发登出的问题) |
| 2026-06-14 | S21: 安全审计第四轮 — 错误响应去敏感化 | ✅ 完成 | internal/handler/study.go(Chat fallback: fallbackErr.Error()改为 log.Printf+通用"AI 服务暂时不可用"提示, streamDirectReply: err.Error()改为 log.Printf+通用提示, 防止 LLM 错误含 API key/endpoint 泄露), internal/handler/material.go(DeleteMaterial: err.Error()改为 log.Printf+通用"删除失败"提示, 防止 DB 错误含 SQL 片段泄露, 新增 log 导入) |
| 2026-06-14 | S22: 安全审计第四轮 — WebSocket writePump 写超时 | ✅ 完成 | internal/handler/ws.go(writePump: 每次 WriteMessage 前增加 SetWriteDeadline(now+10s), 覆盖业务消息/CloseMessage/PingMessage 三种写入, TCP 连接僵死时写超时触发 goroutine 正常退出, 防止 goroutine 泄漏) |
| 2026-06-14 | S23: 安全审计第四轮 — maybeSummarize 异步摘要超时 | ✅ 完成 | internal/handler/study.go(maybeSummarize: context.Background()改为 context.WithTimeout(context.Background(), 30*time.Second)+defer cancel(), 防止后台摘要生成 goroutine 在 LLM API 挂起时无限阻塞) |
| 2026-06-14 | T42: 学习路径规划 — 根据材料生成学习路线图 | ✅ 完成 | internal/agent/pathplanner.go(新: PathPlannerAgent+PathStep+PathPlannerOutput结构体+const pathPlannerPrompt规划原则+Generate方法4返回值), internal/handler/learning_path.go(新: GetLearningPath端点+LearningPathStep前端友好结构含index/material_titles/card_count/reviewed_count+buildPathSummary构建材料摘要上下文(解析analysis_data提取知识点/关系)+savePathTrace记录LLM调用追踪+model name从h.LLM.ModelName获取), main.go(GET /learning-path路由注册), web/src/api/client.js(新增getLearningPath函数), web/src/router/index.js(/learning-path路由), web/src/components/AppLayout.vue(侧栏新增"学习路径"导航项+路线图标SVG), web/src/views/LearningPath.vue(新视图: 竖向时间线+步骤节点圆形按钮+连接线颜色随进度变化+步骤卡片可展开/收起+难度徽章3色+预估时长+卡片复习进度条+前置依赖提示+关联材料列表可点击跳转+开始学习/查看卡片快捷按钮+总体进度概览卡片+完成横幅+骨架屏加载+空状态引导+错误重试+暗色模式适配+sm响应式布局) |
| 2026-06-14 | T43: Quiz 错题本 — 自动收集错题+重练 | ✅ 完成 | internal/model/mistake.go(新: QuizMistake模型含user_id/quiz_id/user_answer/correct_answer/mistake_at/reviewed+UUID自动生成+Quiz关联字段gorm:"-"), internal/database/database.go(迁移QuizMistake), internal/handler/study.go(AnswerQuiz答错时自动upsert到quiz_mistakes: 新建或更新答案+时间+重置reviewed), internal/handler/mistake.go(新: ListMistakes分页含MistakeItem响应结构+批量加载Quiz详情+批量加载材料标题避免N+1, ReviewMistake单条标记已复习, BatchReviewMistakes批量标记, DeleteMistake删除, GetMistakeStats含按难度JOIN统计, RetryMistakes从未复习错题中选取≤20题+附带上次错误答案+mistake_id用于自动标记复习), main.go(GET /mistakes/stats+GET /mistakes+POST /mistakes/retry+POST /mistakes/batch-review+POST /mistakes/:id/review+DELETE /mistakes/:id 路由注册), web/src/api/client.js(新增retryMistakes函数), web/src/views/Mistakes.vue(新视图: 4统计卡片(总错题/待复习/已复习/复习率)+3过滤标签(全部/待复习/已复习)+全选checkbox+批量标记复习+错题卡片(难度徽章+题型徽章+材料标题+复习状态+时间+题目+选择题选项展示含红绿标记+非选择题红绿答案对比+解析+标记已复习/移除操作栏)+分页+错题重练模式(进度条+当前题目+选择题点击作答+填空简答输入+答题结果红绿反馈+自动标记复习+下一题/完成重练+完成统计横幅)+空状态引导+骨架屏+暗色模式适配+sm响应式布局) |
| 2026-06-14 | LaTeX 全应用数学公式渲染 + 4 种题型 + 学科感知出题 | ✅ 完成 | web/src/composables/useMathRender.js(新: renderMath+renderMathWithHighlight 共享 composable+KaTeX 4 种定界符$..$/$$..$$/\(..\)/\[..\]+占位符策略防 HTML 转义冲突), web/src/views/Cards.vue+CardStudy.vue+MaterialDetail.vue+Chat.vue+Quiz.vue(全部 import renderMath/renderMathWithHighlight+v-html 替代{{ }}+scoped dark KaTeX 样式), web/src/views/Quiz.vue(新增 selectedTypes ref+filteredQuizzes computed+toggleType 函数+4 种题型独立 UI: choice 选项按钮/judge ✓正确✗错误大按钮/fill 单行 input/short_answer 5 行 textarea+judge 答案归一化), internal/agent/quizmaster.go(QuizMaster prompt 扩展: 学科判断第一步(理科/工科→计算题为主+计算机→算法设计+文科/社科→论述评价+医学/生物→机制分析+语言→翻译写作)+解答题 6 类(计算/证明/综合分析/算法设计/论述评价/实验机制)+标注适用学科+category 从 int 改为 string 中文分类), internal/agent/tools.go(executeCreateQuiz prompt 同步学科感知指令), internal/agent/analyst.go(Analyst prompt: 关系 type 从英文 prerequisite/related/analogy 改为中文 8 种(依赖/前置条件/包含/关联/基于/对比/推导/实例化)+要求至少 8 条关系), internal/agent/mapbuilder.go(MapBuilder prompt 重写: 必须复用 Analyst 概念关系作为边+category 从 int 0-4 改为中文字符串(核心概念/原理/算法/应用/模型/方法/公式/功能)+GraphNode.Category 类型 int→string+边 label 丰富化), internal/handler/study.go(ExportCards 重写: #html:true+HTML 内联 CSS 样式+Front 含概念名+难度徽章(绿/琥珀/红)+Back 三结构化区块(详解灰底/公式蓝底/记忆技巧琥珀底)+htmlEsc/htmlNL 辅助函数+checkAnswer 新增 quizType 参数+judge 归一化映射(正确/对/T/TRUE/√/YES↔错误/错/F/FALSE/×/NO)), web/src/views/Graph.vue(MapBuilder 复用 Analyst 关系+边 label 显示在 tooltip 和连线文字+边样式区分(对比/关联虚线/其他实线)+demo graph 新增边标签+category 字符串匹配 categoryColors 着色) |
| 2026-06-14 | T44: 材料批量操作 — 多选+批量删除/分析 | ✅ 完成 | internal/handler/material.go (BatchAnalyzeMaterials 并发 goroutine+信号量限流 2 并发+WebSocket 逐个推送 batch_analyze_progress+batch_analyze_complete 事件, BatchDeleteMaterials 逐个事务删除含级联卡片+题目+RAG 向量清理), main.go (POST /materials/batch-analyze+DELETE /materials/batch 路由注册在 :id 之前), web/src/api/client.js (batchAnalyzeMaterials+batchDeleteMaterials), web/src/views/Upload.vue (selectedIds Set+toggleSelection+isAllSelected/isPartialSelected computed+toggleSelectAll+handleBatchAnalyze/handleBatchDelete+每行 checkbox 移动端/桌面端+全选 checkbox+批量操作栏 Transition 动画+进度条+WebSocket batch_analyze_progress/complete 事件处理+暗色模式适配+取消选择按钮) |
| 2026-06-14 | S24: 安全审计第五轮 — Chat 消息长度限制 | ✅ 完成 | internal/handler/study.go(ChatRequest.Message: binding tag 增加 max=10000, 防止超长消息导致 DB 膨胀/LLM 超额/内存压力; ChatStream: message query 参数增加 len>10000 检查并返回 400) |
| 2026-06-14 | S25: 安全审计第五轮 — recommendation N+1 查询消除 | ✅ 完成 | internal/handler/recommendation.go(findUnusedMaterials: 从加载全部材料+循环 N*2 次 COUNT 查询改为单条 SQL INNER JOIN cards+GROUP BY+HAVING SUM(CASE WHEN review_count>0)=0+LIMIT 5, 100 材料时从 201 次查询降为 1 次) |
| 2026-06-14 | S26: 安全审计第五轮 — goroutine panic 恢复防死锁 | ✅ 完成 | internal/agent/orchestrator.go(Phase 2 三个 goroutine 各增加 defer recover(): panic 时向 results channel 发送 error AgentResult, 防止 collection 循环永久阻塞导致调用方 goroutine+activeAnalyses 泄漏) |
| 2026-06-14 | S27: 安全审计第五轮 — Router guard 使用 isLoggedIn | ✅ 完成 | web/src/router/index.js(beforeEach: !auth.token 改为 !auth.isLoggedIn, auth.token 改为 auth.isLoggedIn, 过期 token 在路由导航时即触发 JWT 解码检查+localStorage 清理+跳转登录, 不再等到 API 调用才发现过期) |
| 2026-06-14 | S28: 安全审计第五轮 — Register 输入净化 | ✅ 完成 | internal/handler/auth.go(Register: strings.TrimSpace 裁剪 username+email 防止空白字符绕过唯一性检查), internal/model/user.go(RegisterRequest.Password: binding tag 增加 max=128 防止 bcrypt 72 字节截断+CPU 密集型 DoS) |
| 2026-06-14 | S29: 安全审计第五轮 — Upload 客户端文件验证 | ✅ 完成 | web/src/views/Upload.vue(processFile: 新增 ALLOWED_EXTENSIONS 常量+MAX_FILE_SIZE 20MB+上传前检查文件扩展名(防拖拽绕过 accept)+文件大小(避免上传大文件后才报错), 即时显示错误提示) |
| 2026-06-14 | T45: 学习日历热力图 — GitHub 风格年度学习活跃度 | ✅ 完成 | internal/handler/dashboard.go(新增 GetCalendarHeatmap 端点: 4 活动源聚合查询 chat_messages(JOIN conversations)+quiz_attempts+cards(last_reviewed_at)+materials(created_at), DATE 分组+按 user_id 隔离, 生成全年 365/366 天数据含 level 0-4, fmt+strconv 导入), main.go(GET /stats/calendar 路由注册), web/src/api/client.js(新增 getCalendarHeatmap(year) 函数), web/src/views/Dashboard.vue(替换 30 天 heatmap 为 ECharts calendar 全年热力图: 年份选择器+活跃天数/总活动次数/连续天数统计+GitHub 风格绿色渐变(亮色 #ebedf0→#216e39/暗色 #1e293b→#39d353)+piecewise visualMap 5 级+tooltip 含星期+月份中文标签+周一首日+isDark watcher 重绘+loadCalendarData 年份切换+底部图例少→多+响应式 sm 统计摘要切换) |
| 2026-06-14 | T46: 对话消息代码块增强 — 复制按钮+语言标签+暗色适配 | ✅ 完成 | web/src/views/Chat.vue(自定义 marked.Renderer.code: 代码块包裹 div.code-block-wrapper+span.code-lang 语言标签(40+语言映射含别名)+button.code-copy-btn 复制按钮(SVG图标+文字+data-copy-code encodeURIComponent 编码)+DOMPurify 现代化 allowlist API+ADD_ATTR 白名单含 data-copy-code/button/svg/rect+addCodeCopyButtons 后处理占位+handleCopyCode 事件委托(navigator.clipboard.writeText+textarea fallback+2s 已复制状态+图标切换 checkmark→copy)+copied CSS 类绿色反馈+暗色模式全适配(code-block-wrapper/code-lang/code-copy-btn/pre/inline code 五组 dark 样式)+pre 覆盖 prose 默认配色(slate-800/0f172a)+4px 自定义滚动条+onMounted 无额外监听器(事件委托在模板 @click)) |
| 2026-06-14 | OPT-1: 知识图谱布局优化 — 力导向参数+箭头+标签层级 | ✅ 完成 | web/src/views/Graph.vue(force: repulsion 300→800+edgeLength [80,200]→[120,250]+gravity 0.1→0.15+新增 friction 0.2 加速收敛, edgeSymbol ['none','arrow']+edgeSymbolSize [0,8] 方向箭头, 边标签默认隐藏 hover 显示(label.show false+emphasis.edgeLabel.show true), 小节点标签隐藏(symbolSize>=40 才显示), resetNodeStyles 同步更新) |
| 2026-06-14 | OPT-2: 学习路径加载修复 — 超时保护+空状态+骨架提示 | ✅ 完成 | web/src/views/LearningPath.vue(15s setTimeout 超时安全网自动清除 loading 并显示错误提示, 骨架屏底部 animate-pulse 提示文字"AI 正在分析...", 空状态条件从 && 改为 \|\| 确保无材料时立即显示引导页, loadingTimer 模块级变量+finally 块 clearTimeout 清理) |
| 2026-06-14 | OPT-3: 卡片切换速度提升 50% — 动画时间优化 | ✅ 完成 | web/src/views/CardStudy.vue(flip-card-inner transition 0.6s→0.3s, cardStyle slide transition 0.35s→0.18s, doNav 两阶段 setTimeout 360+360→180+180, 复习后延迟 setTimeout 250→120, 总复习→下一张时间 ~970ms→~480ms) |
| 2026-06-14 | OPT-4: Quiz 难度与题型优化 — 分布比例+计算/应用区分+后置校验 | ✅ 完成 | internal/agent/quizmaster.go(难度分布 easy 25%/medium 40%/hard 35%+hard≥3道, 数学计算题 vs 工程应用题区分指令各占50%+具体数值要求+工程场景示例, 解答题必须 medium/hard 禁止 easy+hard≥2道), internal/agent/tools.go(executeCreateQuiz 精简 prompt 删除与系统提示重复内容+仅传请求参数, 后置校验 difficulty/type 合法性+解答题 easy→medium 自动修正) |
| 2026-06-14 | T47: 材料标签系统 — 自定义标签+按标签过滤 | ✅ 完成 | internal/model/material.go(Material 新增 tags 字段 string size:500 逗号分隔+UploadRequest 新增 Tags 字段), internal/handler/material.go(UploadMaterial 传递 Tags+ListMaterials 新增 tag 查询参数 LIKE 过滤+GetTags 端点: 查询用户所有材料 tags 字段拆分计数排序返回), internal/handler/study.go(GlobalSearch 材料搜索新增 tags LIKE 匹配+subtitle 显示标签), main.go(GET /tags 路由注册在 materials 之前), web/src/api/client.js(新增 getTags 函数), web/src/views/Upload.vue(标签输入增强: focus 显示已有标签建议下拉+使用次数+过滤已输入+点击追加+parsedFormTags pills 可删除, 标签过滤栏: pill toggle 多选+activeTagFilter+全部按钮+各标签计数, 材料卡片显示 tags: 移动端+桌面端 title 下方小标签, handleUpload 修复 tags 发送格式从数组改为逗号字符串, loadMaterials 传 tag 过滤参数, loadTags+watch activeTagFilter 自动刷新, tag-suggest Transition 动画), web/src/views/Cards.vue(listMaterials 导入+materialTagMap ref+loadMaterialTags 函数+onMounted 并行加载, 卡片正面 tags 区域新增竖线分隔+材料标签 indigo 小标签), web/src/views/Quiz.vue(listMaterials 导入+quizMaterialTagMap ref+Promise.all 并行加载 quizzes+materials, 题目头部难度徽章后新增材料标签 indigo 小标签), web/src/components/GlobalSearch.vue(placeholder 新增"标签"+初始提示新增"标签"标签) |
| 2026-06-14 | OPT-5: Out of Memory 崩溃修复 — WebSocket 泄漏+列表去重大字段 | ✅ 完成 | internal/handler/material.go(ListMaterials 新增 Select 排除 content/analysis_data/graph_data 三大字段防止 MB 级数据涌入前端), web/src/views/Upload.vue(connectWebSocket 开头关闭旧连接防孤儿 WS, analysis_complete/batch_analyze_complete/error 三处主动关闭 WS 释放资源) |
| 2026-06-14 | OPT-6: 知识图谱方框化 — 圆圈节点改为圆角矩形 | ✅ 完成 | web/src/views/Graph.vue(initChart: symbol 改 roundRect+symbolSize 按文字长度动态计算[width,height]+label 始终显示白字加粗+itemStyle 加 borderRadius 6+force repulsion 800→1200+edgeLength [120,250]→[150,300]+gravity 0.15→0.12, resetNodeStyles/applySearchHighlight/highlightNodeConnections/highlightCrossMaterial 全函数同步 roundRect+borderRadius 样式) |
| 2026-06-14 | OPT-7: 知识卡片单击放大+双击翻转 — 模态框大卡片视图 | ✅ 完成 | web/src/views/Cards.vue(新增 enlargedCard/modalFlipped ref+cardClickTimer 250ms 延迟区分单击双击+onCardClick 单击延迟打开模态+onCardDblClick 双击原地翻转+closeEnlarged 关闭, Teleport 模态框: 全屏半透明遮罩+max-w-lg 520px 高圆角大卡片+正面完整内容+背面公式/记忆技巧/复习按钮+点击翻转+@click.stop 防冒泡+ESC 关闭+modal-fade 过渡动画), 副标题改为"单击放大 · 双击翻转查看详情" |
| 2026-06-14 | OPT-8: 卡片切换速度再提升 — 动画时间进一步压缩 | ✅ 完成 | web/src/views/CardStudy.vue(flip-card-inner transition 0.3s→0.2s, cardStyle slide transition 0.18s→0.1s, doNav 两阶段 setTimeout 180+180→100+100, 复习后延迟 120→60, 总复习→下一张时间 ~480ms→~260ms) |
| 2026-06-14 | OPT-9: 练习题三级提示生成修复 — 从空提示到实际生成 | ✅ 完成 | internal/agent/quizmaster.go(QuizItem 结构体新增 Hint1/Hint2/Hint3 字段+quizMasterPrompt JSON schema 新增 hint_1/hint_2/hint_3+新增三级提示生成要求说明), internal/agent/orchestrator.go(saveQuizzes 映射 Hint1/Hint2/Hint3 到 model.Quiz), internal/agent/tools.go(executeCreateQuiz 映射 Hint1/Hint2/Hint3+prompt 新增三级提示要求) |
| 2026-06-14 | OPT-10: 答题后取消自动跳转 — 改为手动下一题 | ✅ 完成 | web/src/views/Quiz.vue(submitAnswer 移除 1500ms 自动跳转下一题 setTimeout, 保留全部答完自动显示报告改为 2000ms 延迟, 用户可充分阅读解析后手动点击"下一题") |
| 2026-06-14 | T48: 通知系统 — 间隔重复提醒+分析完成通知+成就解锁通知 | ✅ 完成 | internal/model/notification.go(新: Notification模型含user_id/type/title/body/action_url/read_at/created_at+UUID自动生成), internal/database/database.go(迁移Notification), internal/handler/notification.go(新: ListNotifications含惰性生成+ReadNotification+ReadAllNotifications+GetUnreadNotificationCount+generatePendingNotifications三源检查: 待复习卡片/分析完成/成就解锁+每日去重+CreateNotification公共方法), main.go(4条通知路由: unread-count/read-all/notifications列表/notifications/:id/read), web/src/api/client.js(新增getNotifications/getUnreadNotificationCount/readNotification/readAllNotifications), web/src/components/AppLayout.vue(侧栏+移动端通知铃铛+未读红点徽章+Teleport通知面板: 头部未读计数+全部已读按钮+通知列表含类型图标三色区分+相对时间+未读蓝点+点击标记已读并跳转+空状态+加载spinner+notif-fade过渡动画+自定义滚动条+60s轮询unreadCount+Esc关闭面板+暗色模式全适配+scoped CSS) |
| 2026-06-14 | OPT-11: 知识图谱关系标签常驻显示 — 边标签默认可见 | ✅ 完成 | web/src/views/Graph.vue(边 label.show 从 false 改为 !!e.label 有标签即显示, 字号11px加粗+紫色主题色+半透明背景圆角pill提升可读性, 连线加粗至2px+透明度0.9+curveness 0.2, resetNodeStyles 同步更新) |
| 2026-06-14 | OPT-12: Quiz 难度分布调优 — easy 30%/medium 40%/hard 30% | ✅ 完成 | internal/agent/quizmaster.go(难度分布从 25/40/35 调整为 30/40/30, easy 3-5道/medium 4-8道/hard 3-5道), internal/agent/tools.go(executeCreateQuiz 指定难度占比从 50% 提升至 60%, 其余为相邻难度) |
| 2026-06-14 | T49: 卡片书签与笔记 — 个人标注系统 | ✅ 完成 | internal/model/card.go(新增 is_bookmarked bool+user_note text 字段), internal/handler/study.go(新增 ToggleBookmark 切换书签+UpdateCardNote 更新笔记+ListCards bookmarked=true 过滤+ExportCards 新增 Notes 列4列CSV), main.go(PUT /cards/:id/bookmark+PUT /cards/:id/note 路由注册), web/src/api/client.js(新增 toggleBookmark+updateCardNote), web/src/views/Cards.vue(书签过滤按钮amber pill toggle+bookmarkCount计数+卡片正面书签图标amber toggle+卡片背面笔记textarea 800ms防抖保存+模态框正面书签图标+模态框背面笔记输入区+仅书签空状态+filteredCards bookmarked 过滤+暗色模式适配) |
| 2026-06-14 | T50: 材料导入增强 — Markdown/Word 文件上传+上传进度条+文件类型图标+大小警告 | ✅ 完成 | web/src/api/client.js(uploadFile 新增 onProgress 回调参数+axios onUploadProgress 配置), web/src/views/Upload.vue(uploadProgress/fileSizeWarning ref+processFile 传递进度回调+>10MB 警告提示+上传进度条 UI 含百分比显示+SVG 文件类型图标替换 emoji(PDF红/Word蓝/MD绿/TXT灰)+clearFile 清理新状态), internal/handler/fileparser.go(已支持 .md/.docx/.pdf/.txt/.markdown 无需改动) |
| 2026-06-14 | T51: 多 Agent 辩论模式 — 多视角讨论知识点 | ✅ 完成 | internal/agent/debate.go(新: DebateOrchestrator+DebateMessage/DebateOutput结构体+三角色系统提示(analystDebateSystem/quizMasterDebateSystem/cardMakerDebateSystem/debateSummarySystem)+RunDebate方法: Round1 Analyst分析本质→Round2 QuizMaster出题视角(含Round1上下文)→Round3 CardMaker记忆策略(含Round1+2上下文)→Summary综合总结+ChatWithUsage 4返回值+错误降级), internal/handler/debate.go(新: DebateRequest含concept binding验证+StartDebate端点: 创建DebateOrchestrator→RunDebate→记录LLMTrace→返回DebateOutput), main.go(POST /debate路由注册), web/src/api/client.js(新增startDebate函数+120s超时), web/src/views/Debate.vue(新视图: 聊天式辩论界面+底部concept输入框+推荐主题8个pill按钮+消息卡片按Agent着色(Analyst蓝/QuizMaster琥珀/CardMaker翠绿/Summary紫)+圆形头像+角色徽章+轮次分隔线+Markdown渲染(marked+DOMPurify)+逐条300ms动画展开+加载状态(轮次推断+Agent旋转图标+提示文字)+错误状态+重试按钮+新辩论按钮+暗色模式全适配+响应式p-4 sm:p-6 lg:p-8+message-in入场动画+custom-scroll滚动条), web/src/router/index.js(/debate路由注册在learning-path和chat之间), web/src/components/AppLayout.vue(侧栏新增"Agent 辩论"导航项+双气泡SVG图标+位于学习路径和AI对话之间) |
| 2026-06-14 | T52: Agent 质量评分 — LLM-as-Judge 前端可视化 | ✅ 完成 | internal/model/trace.go(LLMTrace 新增 JudgeComment string size:1000 字段), internal/agent/orchestrator.go(AgentResult 新增 JudgeComment+collectResult 保存 evalResult.Feedback+saveTrace 传播 JudgeComment+日志含三维度分数), internal/handler/dashboard.go(GetMetrics 新增 quality_distribution 四区间统计+judge_comments 按Agent分组每组3条), web/src/components/AgentFlow.vue(5星级评分SVG+half-star支持+starClass函数+Judge评语hover tooltip含info图标+暗色适配), web/src/views/Upload.vue(agents新增judgeComment字段+WebSocket传递judge_comment+reset清理), web/src/views/Dashboard.vue(质量分布条4色分段红/琥珀/绿/靛蓝+qualityDistPct/qualityDistTotal/scoreColorClass辅助+最近评语列表5条含Agent标签+分数徽章+custom-scroll+柱状图tooltip增强含Judge评语+暗色适配) |
| 2026-06-14 | T53: 自适应出题 — 根据用户水平动态调整难度 | ✅ 完成 | internal/handler/study.go(新增analyzeUserLevel: 查询最近20次quiz_attempts按正确率分三档(>80%→medium/hard,<50%→easy/medium,50-80%→全难度)+GetDifficultyLevel端点: 返回accuracy/total_attempts/recommended_difficulty/reason+各难度答题统计(difficulty_stats JOIN quizzes)+各难度题目总数(quiz_counts), ListQuizzes新增recommended=true参数: 按推荐难度过滤+响应附赠recommended/recommended_difficulty/recommended_reason元数据), main.go(GET /quizzes/difficulty-level路由注册在/quizzes之前), web/src/api/client.js(新增getDifficultyLevel函数), web/src/views/Quiz.vue(studyMode ref('all'/'recommended')+difficultyInfo ref+loadDifficultyInfo异步获取难度信息+switchMode(mode)切换模式(重置状态+重新加载题目+推荐模式额外加载difficultyInfo)+diffBadgeClass辅助函数+模板: 模式切换按钮(全部题目/智能推荐含闪电图标)+推荐横幅(渐变背景+正确率圆环+已答题数+推荐难度徽章+各难度统计条(正确数/总数+百分比))+每次答题后异步刷新difficultyInfo+空状态区分推荐/全部模式+切换提示), internal/middleware/middleware_test.go(makeToken自动补全iss/aud修复S14后测试失败) |
| 2026-06-14 | T54: 环境配置校验 — 启动时自动检查必需配置 | ✅ 完成 | config.go(重写: LoadConfig公开函数+defaultConfig全字段默认值+applyEnvOverrides支持18个环境变量覆盖+Validate增强: 8项校验含必填/端口/DB路径/LLM BaseURL/Qdrant/RAG合理性+友好错误列表+提示.env.example, PrintSummary格式化配置摘要含敏感值遮蔽+环境变量检测, GetConfigPath支持CONFIG_PATH环境变量, maskSecret保留), main.go(LoadConfig替代loadConfig+GetConfigPath+配置失败友好错误提示Fprintf(os.Stderr)+os.Exit(1)替代log.Fatalf+cfg.PrintSummary替代单行log), .env.example(新: 完整环境变量文档含8大类20+变量+优先级说明+获取方式注释), config.yaml.example(增强: 每个字段标注对应环境变量名) |
| 2026-06-14 | Graph Redesign: 知识图谱可视化重构 — 双模式布局+卡片式节点 | ✅ 完成 | web/src/views/Graph.vue(重写: layoutMode ref('hierarchical'/'force')+toggle按钮组+computeHierarchicalLayout层次布局算法(BFS分层+重心排序+坐标计算)+layout:'none'显式坐标+卡片式节点buildCardNode(160×56px roundRect+LinearGradient色条+rich text label含icon/name/desc)+buildEdgeConfig边样式(源节点着色+药丸标签+小箭头)+getCategoryColor/buildCardNode/buildEdgeConfig共享helper消除250行重复代码+categoryIcons 14类Unicode符号+搜索/高亮/跨材料/右键菜单/PNG导出/暗色模式全功能保留) |
| 2026-06-14 | T55: README 与项目文档完善 | ✅ 完成 | README.md(全面重写: 项目简介+9大功能特性模块详述+技术架构表11行+快速开始5步含配置表格(必需/可选环境变量)+Seed演示数据含完整curl示例+Docker部署+API概览8大分类40+端点表格+完整项目结构目录树(含所有新增文件)+截图占位6张+测试命令+CI/CD说明+配置参考链接+贡献指南含开发规范+MIT许可证) |
| 2026-06-14 | OPT-13: 学习路径加载修复 — JSON 解析鲁棒性+90s 超时+30 分钟缓存+前端超时适配 | ✅ 完成 | internal/agent/orchestrator.go(新增 extractJSON 辅助函数: 去除 LLM 输出的 ```json 代码块包裹+首尾非 JSON 文本修剪), internal/agent/pathplanner.go+analyst.go+cardmaker.go+quizmaster.go+mapbuilder.go(全部 Agent 的 json.Unmarshal 改用 extractJSON 预处理, 错误日志截断原始输出前200字), internal/handler/auth.go(Handler 新增 pathCacheMu sync.RWMutex+pathCache map[string]*pathCacheEntry+NewHandler 初始化), internal/handler/learning_path.go(新增 pathCacheEntry 结构体+pathCacheTTL 30min 常量, GetLearningPath 重写: 先查缓存→force=true 跳过缓存→context.WithTimeout 90s→LLM 调用→缓存成功结果→区分超时/普通错误返回不同 HTTP 状态码), web/src/api/client.js(getLearningPath 新增 force 参数+120s timeout), web/src/views/LearningPath.vue(generatePath 接受 force 参数→force 时 loading=true+清空 steps→传 force 给 API, 重新生成/重试按钮调用 generatePath(true), 加载超时从 15s 提升到 120s, 骨架屏提示"通常需要 30-90 秒") |

## 当前状态

- **后端**: Go build 通过, 149+ 测试通过 (handler/model/rag/memory/middleware), 29 项安全/质量修复, 全面环境配置校验 (T54: LoadConfig 支持18个环境变量覆盖+CONFIG_PATH+Validate 8项校验+PrintSummary格式化摘要+config.yaml不存在时自动降级), AI 学习建议推荐系统 (T40), 渐进式练习题提示系统 (T41), 学习路径规划 Agent (T42), Quiz 错题本自动收集+重练系统 (T43), 材料批量操作 (T44), 全年学习日历热力图 (T45: 4 活动源聚合+GitHub 风格 ECharts calendar), 学科感知出题系统, Anki 导出 HTML 美化, 材料标签系统 (T47), ListMaterials 去重三大字段防 OOM (OPT-5), 通知系统 (T48: 三类型通知+惰性生成+CRUD), Quiz 三级提示生成修复 (OPT-9), 难度分布调优 30/40/30 (OPT-12), 卡片书签与笔记 (T49: ToggleBookmark+UpdateCardNote+bookmarked 过滤+Anki Notes 列), 多 Agent 辩论模式 (T51: DebateOrchestrator 三轮顺序辩论+综合总结), Agent 质量评分可视化 (T52: LLMTrace JudgeComment 持久化+collectResult 保存 Feedback+GetMetrics quality_distribution+judge_comments), 自适应出题 (T53: analyzeUserLevel 最近20题正确率分析+GetDifficultyLevel端点+ListQuizzes recommended=true过滤)
- **前端**: Vite build 通过, 25 测试通过 (Toast/ErrorBoundary), DOMPurify XSS 防护, JWT 客户端过期检查+SSE 401 拦截+路由守卫 isLoggedIn, 卡片滑动学习模式 (T38), 知识图谱双模式可视化 (Graph Redesign: 层次/力导布局切换+卡片式节点160×56px含icon/name/desc+LinearGradient色条+rich text label+源节点着色边+BFS层次布局算法+共享helper消除250行重复), 学习路径时间线 (T42: 15s 超时安全网+骨架屏提示+空状态引导), 错题本+重练 (T43), 批量操作 (T44), GitHub 风格全年日历热力图 (T45), 代码块复制按钮+语言标签 (T46), KaTeX 公式渲染, 4 种题型, 客户端文件验证, 材料标签系统 (T47), WebSocket 生命周期管理防内存泄漏 (OPT-5), 知识卡片单击放大+双击翻转模态框 (OPT-7), 学习模式卡片切换 ~260ms (OPT-8), 答题后手动下一题 (OPT-10), 通知铃铛+未读面板+全部已读 (T48: Teleport 面板+60s 轮询+Esc 关闭+暗色适配), 卡片书签与笔记 (T49: 书签图标toggle+笔记textarea 800ms防抖+书签过滤+Anki Notes导出), 材料导入增强 (T50: axios onUploadProgress 进度条+SVG 文件类型图标 PDF红/Word蓝/MD绿/TXT灰+>10MB 文件大小警告), 多 Agent 辩论 (T51: 聊天式辩论界面+三角色着色+Markdown渲染+逐条动画+推荐主题), Agent 质量评分可视化 (T52: AgentFlow 5星级评分+Judge评语tooltip+Dashboard 质量分布条+评语列表+柱状图tooltip增强), 自适应出题 (T53: 全部题目/智能推荐模式切换+推荐难度横幅+正确率/已答题统计+各难度分布条+答题后实时刷新)
- **CI/CD**: GitHub Actions 流水线配置完成 (Go Build&Test + Vue Build&Test + Go Lint)
- **文档**: README.md 全面重写 (T55: 9大功能模块+技术架构表+快速开始5步+配置表格+API概览40+端点+完整目录树+截图占位+贡献指南+开发规范), .env.example 完整环境变量文档, config.yaml.example 字段注释
- **安全**: CORS/WebSocket 白名单 + IP 防欺骗 + JWT iss/aud 防重放 + 空值拒绝 + 级联事务 + 文件 DoS 防护(服务端+客户端双验证) + 内存泄漏清理 + 并发守卫 + 401 凭证清理(axios+SSE 双通道) + 密码内存清除 + 错误响应去敏感化 + WebSocket 写超时 + 异步摘要超时 + 消息长度限制(POST+GET 双通道) + N+1 查询消除 + goroutine panic 恢复 + 注册输入净化(trim+bcrypt DoS 防护)
- **测试**: Go test + Vitest 全绿
- **Go 路径**: E:\bin\go.exe
- **项目路径**: C:\Users\wk的华硕天选5pro\Desktop\go项目\studyforge-pro
- **关键架构**: Go + Gin 后端 / Vue 3 + Vite + TailwindCSS + Pinia 前端 / 两阶段 Agent 流水线 (Analyst全文→精炼上下文→3 Agent并发) / WebSocket 实时推送 / RAG 语义搜索 / ECharts 可视化 / SSE 流式对话 + Function Calling / 学习成就系统 18 种徽章 / IP 级令牌桶限流 / OpenAPI 风格接口文档 / 卡片滑动学习模式 (手势翻转+键盘快捷键) / 跨材料知识图谱 (合并所有材料+分组着色+跨材料关系高亮+双模式布局: 层次BFS分层+力导向物理模拟+卡片式节点含icon/name/desc) / AI 学习建议推荐 (5维分析: 待复习/薄弱知识点/未学习材料/薄弱题型/每日鼓励) / 渐进式练习题提示系统 (3级提示: 方向/线索/接近答案) / 学习路径规划 (PathPlanner Agent+竖向时间线+前置依赖+进度追踪) / Quiz 错题本 (自动收集+重练模式+批量操作) / KaTeX 数学公式渲染 (4种定界符+共享composable) / 学科感知出题 (5类学科+6种解答题型) / 4种题型独立UI (选择/判断/填空/解答+题型过滤) / Anki HTML美化导出 / 材料批量操作 (多选checkbox+并发批量分析+WebSocket进度推送+事务批量删除) / GitHub 风格全年学习日历热力图 (ECharts calendar+4活动源聚合+年份选择器+连续天数统计) / 代码块复制按钮+语言标签 (marked.Renderer自定义+40+语言映射+事件委托+DOMPurify allowlist) / 材料标签系统 (逗号分隔存储+GetTags聚合+ListMaterials tag过滤+Cards/Quiz材料标签显示+GlobalSearch标签匹配) / 卡片书签与笔记 (is_bookmarked+user_note+ToggleBookmark+UpdateCardNote+书签过滤+Anki Notes列导出) / 多 Agent 辩论模式 (DebateOrchestrator 三轮顺序辩论+综合总结+聊天式界面+四角色着色) / Agent 质量评分可视化 (LLM-as-Judge 评语持久化+星级评分+质量分布+评语列表) / 自适应出题 (analyzeUserLevel最近20题分析+三档难度推荐+GetDifficultyLevel端点+ListQuizzes recommended过滤+前端模式切换+实时刷新)

## 注意事项

- `latency_ms` 字段有 `gorm:"-"`，数据库列名是 `latency`（纳秒），查询需除以 1e6 转毫秒
- Agent 方法签名已改为 4 返回值：`(output, inputTokens, outputTokens, error)`
- Toast 系统使用 composables/useToast.js 的 useToast() 和 useConfirm()
- 所有 DB 查询必须按 user_id 隔离
- **【安全-勿回退】** CORS 中间件已改为白名单模式（allowedOrigins map），禁止改回 `Access-Control-Allow-Origin: *`
- **【安全-勿回退】** WebSocket CheckOrigin 已改为白名单校验，禁止改回 `return true`
- **【安全-勿回退】** main.go 已设置 `r.SetTrustedProxies(nil)`，禁止删除
- **【安全-勿回退】** JWT 中间件已拒绝空 userID，ValidateToken 也已拒绝空 userID，禁止回退
- **【安全-勿回退】** config.yaml 禁止写入真实 API key 或 JWT secret（已加入 .gitignore）
- **【安全-勿回退】** memories map 类型为 `map[string]*memoryEntry`（atomic.Int64），禁止改回 `map[string]*memory.ConversationMemory`
- **【安全-勿回退】** DeleteMaterial/DeleteConversation 已用 DB.Transaction 包裹级联删除，禁止改为裸 Delete
- **【安全-勿回退】** fileparser.go 的 io.ReadAll 已加 io.LimitReader(50MB)，禁止移除
- **【安全-勿回退】** Chat.vue 的 renderMarkdown 已加 DOMPurify.sanitize，禁止移除净化步骤
- **【安全-勿回退】** AnalyzeMaterial 已加 activeAnalyses sync.Map 并发守卫，禁止移除
- **【安全-勿回退】** ValidateToken 已加 jwt.WithIssuer+jwt.WithAudience 校验，禁止移除
- **【安全-勿回退】** client.js 401 拦截器已同时清除 token 和 user，禁止只清除 token
- **【安全-勿回退】** auth.js isLoggedIn 已解码 JWT 检查过期，禁止改回 !!token.value
- **【T40 要点】** recommendation.go 的 findWeakQuizType 使用 JOIN quizzes ON quiz_attempts.quiz_id 跨表聚合，注意 user_id 过滤在 quiz_attempts 层；findUnusedMaterials 使用 N+1 查询模式（遍历材料查卡片复习次数），材料数量多时可能需要优化为单条 SQL
- **【安全-勿回退】** auth.js register 已直接使用注册接口返回的 token，禁止再用密码二次登录
- **【安全-勿回退】** Login.vue 登录成功后已清除 form.password，禁止移除
- **【安全-勿回退】** orchestrator.go saveCards/saveQuizzes 已改为 CreateInBatches 批量插入，禁止改回逐条 Create
- **【安全-勿回退】** chatStream SSE fetch 已手动处理 401 自动登出（清除 token+user+跳转 /login），禁止移除
- **【安全-勿回退】** study.go Chat/streamDirectReply 和 material.go DeleteMaterial 错误响应已去敏感化（log.Printf+通用提示），禁止改回暴露原始 err.Error()
- **【安全-勿回退】** ws.go writePump 每次 WriteMessage 前已加 SetWriteDeadline(10s)，禁止移除
- **【安全-勿回退】** study.go maybeSummarize 已加 context.WithTimeout(30s)，禁止改回裸 context.Background()
- **【安全-勿回退】** ChatRequest.Message 已加 binding:"required,max=10000"，ChatStream 已加 len>10000 检查，禁止移除长度限制
- **【安全-勿回退】** recommendation.go findUnusedMaterials 已改为单条 SQL JOIN+GROUP BY，禁止改回 N+1 循环查询
- **【安全-勿回退】** orchestrator.go Phase 2 三个 goroutine 已有 defer recover() 防 panic 死锁，禁止移除
- **【安全-勿回退】** router/index.js beforeEach 已使用 auth.isLoggedIn（JWT 解码+过期检查），禁止改回 auth.token
- **【安全-勿回退】** Register handler 已 TrimSpace username+email，Password 已加 max=128，禁止移除
- **【安全-勿回退】** Upload.vue processFile 已有客户端文件类型+大小验证，禁止移除
- Handler 新增 `analysisProgress` map（sync.RWMutex 保护），key 为 materialID，分析完成 30 秒后自动清理
- Handler 新增 `activeAnalyses` sync.Map（key: materialID），防止同一材料重复触发分析 goroutine
- Handler 新增 `loadingKeys` sync.Map（key: convID），防止同一对话并发加载 DB 消息
- AgentResult 新增 `MaterialID` 字段，orchestrator 在收集结果时自动设置
- 前端 Upload.vue 支持 WebSocket 断线轮询 fallback（每 3 秒轮询 GET /api/materials/:id/status）
- ChatStream (GET /api/chat/stream) 采用两阶段策略：先非流式 ChatWithTools 检测工具调用 → 若需要工具则执行循环（发送 [TOOL_CALL]/[TOOL_RESULT] SSE 事件）→ 最后 ChatStreamFromMessages 流式输出最终回复；若无需工具则走 streamDirectReply 快速路径
- LLMClient 新增 ChatStreamFromMessages 方法：基于完整消息历史（含 tool results）进行流式对话
- 前端 chatStream 第 6 个参数为 onToolEvent 回调，消息对象新增 toolEvents 数组（含 name/done/argsStr），模板用 spinner + checkmark 渲染工具状态面板
- ConversationMemory 新增 roundCount 计数，每次 assistant 回复 IncrementRound()；每 10 轮自动异步调用 LLM 生成压缩摘要（maybeSummarize），合并已有长期摘要 + 短期缓冲，通过 UpdateSummary 写入；BuildContext 第一层【历史对话摘要】将自动包含该摘要
- Cards.vue 搜索功能：客户端 computed 过滤（concept/detail/tags/formula/memory_tip 五字段模糊匹配），搜索关键词用 <mark> 黄色高亮，匹配标签变色（primary），带结果计数和"搜索无结果"空状态
- Card 模型新增间隔重复字段：review_count(累计复习次数)、next_review_at(下次复习时间)、ease_factor(SM-2难度因子,默认2.5,范围1.3-3.0)、interval_days(当前间隔天数)、last_reviewed_at(上次复习时间)
- ReviewCard 端点 POST /api/cards/:id/review：body `{"result":"mastered"|"review"}`。mastered 递增间隔(1→3→interval*ease)并提高ease；review 重置间隔为1天并降低ease
- ListCards 新增 `due=true` 查询参数，过滤 next_review_at <= now 或未复习的卡片
- 前端 Cards.vue 背面底部新增"再复习/已掌握"双按钮（@click.stop 阻止翻转），上方显示间隔/系数/下次复习时间，正面右上角显示复习状态徽章
- MaterialDetail.vue (T06)：通过 getMaterial(id) 获取预加载 Cards+Quizzes 的材料数据，解析 analysis_data JSON 展示 Analyst 摘要/知识点/关系/重要性，底部展示关联卡片网格和练习题列表，支持"重新分析"按钮和"知识图谱"跳转
- Quiz.vue (T07)：_answerHistory 对象记录每题答案/选中索引/正确性/用时；goToQuestion() 支持任意跳转；导航点用 dotClass 显示绿(对)/红(错)/蓝(当前)；计时器用 setInterval 每秒更新 elapsed；提交后 1.5s 自动跳到下一未答题；totalTime 累计所有题目用时
- Dashboard.vue (T09)：loading ref 控制骨架屏/真实内容切换；骨架屏使用 .skeleton + .skeleton-box CSS 类，shimmer 渐变动画（1.4s 周期，从右到左扫过）；模板用 v-if="loading" / v-else 双分支，骨架屏完整镜像真实布局（统计卡片×4、图表两行、材料列表、AI 概览）；onMounted 数据加载完或 catch 异常均会设置 loading=false
- NotFound.vue (T10)：catch-all 路由 `:pathMatch(.*)*` 放在 AppLayout children 最后，未登录用户访问任意 URL 会先被 beforeEach 重定向到 /login；已登录用户访问未知 URL 显示 404 页面（保留侧边栏）。页面显示当前路径、返回首页/返回上页按钮、4 个快捷入口（上传/卡片/测验/对话）
- ExportCards (T11)：GET /api/cards/export 支持 material_id 和 difficulty 过滤。输出 Anki 兼容 tab-separated CSV（#separator:tab 头声明），含 UTF-8 BOM 保证 Excel 中文正确显示。Front=concept，Back=detail+formula+memory_tip 拼接，Tags=逗号转分号。Content-Disposition attachment 触发浏览器下载。路由 /cards/export 注册在 /cards/:id 之前避免参数匹配冲突
- Cards.vue 导出按钮 (T11)：使用 fetch + Blob + URL.createObjectURL + 临时 a 标签触发下载，不经过 axios（因为需要 blob 响应），带 JWT Authorization 头。exporting ref 控制按钮禁用状态，下载完成后 Toast 提示
- Graph.vue PNG 导出 (T11)：使用 ECharts 内置 getDataURL({ type: 'png', pixelRatio: 2, backgroundColor: '#fff' }) 生成 2x 高清 base64 图片，直接赋值给 a.href 触发下载。文件名包含材料标题和日期
- Conversation 模型 (T12)：conversations 表（id/user_id/title/created_at/updated_at）+ chat_messages 表（id/conversation_id/role/content/created_at），UUID 主键，GORM 自动迁移
- Conversation CRUD 端点 (T12)：GET /api/conversations（列表含消息数，LEFT JOIN 统计）、GET /api/conversations/:id（含完整消息列表按时间排序）、POST /api/conversations（创建）、PUT /api/conversations/:id（改标题）、DELETE /api/conversations/:id（级联删除消息+清理内存记忆）
- Chat/ChatStream 持久化 (T12)：两个端点均支持 conversation_id 参数（不传则自动创建新对话，标题取首条消息前 50 字）。用户消息在 LLM 处理前保存，AI 回复在所有输出路径（正常流式/工具循环/降级 fallback）完成后保存。每次消息保存后更新对话的 updated_at
- ChatStream [CONV_ID] SSE 事件 (T12)：流式响应首先发送 `data: [CONV_ID]{"conversation_id":"xxx"}` 事件，前端收到后设置 currentConvId 并刷新侧栏列表。前端 chatStream 新增第 7、8 参数：conversationId（传入 URL 参数）和 onConvId 回调
- getOrCreateMemory 改为按 conversation_id 键值 (T12)：memories map 的 key 从 userID 改为 convID，每个对话有独立记忆上下文。loadMemoryFromDB 方法从 chat_messages 表加载最近 20 条消息到 ConversationMemory 的短期缓冲，恢复多轮对话上下文
- Chat.vue 对话历史侧栏 (T12)：左侧 w-64 面板显示对话列表（标题+消息数），点击切换（getConversation 加载消息），"新对话"按钮清空当前状态，hover 显示删除按钮（useConfirm 确认删除）。发送消息后 loadConversations() 刷新列表。不再使用 localStorage，全部从后端持久化加载
- Graph.vue 搜索框 (T13)：浮层搜索输入框位于图谱容器左上角，200ms 防抖，按节点 name/category/description 三字段模糊匹配。匹配节点：黄色边框+阴影+放大 1.2 倍；未匹配节点：opacity 0.25 半透明。匹配计数实时显示，支持清除搜索。搜索状态在图表重绘后自动恢复
- Graph.vue 缩放适应 (T13)：顶部工具栏"适应"按钮，调用 chart.dispatchAction({ type: 'restore' }) 重置缩放和平移到初始状态。恢复后若有搜索词则 300ms 后重新应用高亮
- Graph.vue 节点高亮关联边 (T13)：点击节点后，该节点金色边框+阴影，关联节点紫色边框+阴影，非关联节点 opacity 0.2；关联边变紫色加粗(3px)，非关联边变细(1.5px) opacity 0.1。下方详情面板显示关联节点列表，每个可点击聚焦跳转
- Graph.vue 右键菜单 (T13)：chart 'contextmenu' 事件触发，定位到鼠标位置。4 个选项：查看详情（设置 selectedNode+高亮关联）、高亮关联（同上）、缩放到此节点（dispatchAction highlight）、隐藏节点（加入 hiddenNodes Set 并重建图表）。全局 click 事件自动关闭菜单
- Graph.vue 动态图例 (T13)：右上角浮层显示当前图谱中出现的所有分类及对应颜色圆点，从节点数据动态计算。categoryColors 预设扩展到覆盖更多分类类型
- PWA manifest.json (T14)：应用名称"StudyForge Pro - AI 智能学习平台"，short_name "StudyForge"，theme_color #4f46e5，display standalone。icons 含 192/512 PNG + 512 maskable + SVG any。shortcuts 3 个快捷入口（上传/卡片/对话）
- Service Worker sw.js (T14)：两个缓存桶 studyforge-v1（动态）+ studyforge-static-v1（静态预缓存）。Install 预缓存 manifest/offline/icons；Activate 清理旧缓存+claim；Fetch 策略：API→Network-First(503 JSON fallback)，静态资源(JS/CSS/字体/图片/assets/)→Cache-First，导航→Network-First+离线页回退。支持 SKIP_WAITING/CLEAR_CACHE 消息通信
- offline.html (T14)：渐变背景离线页面，显示 WiFi-off 图标+"当前处于离线状态"提示+重新连接按钮。监听 online 事件自动刷新
- index.html PWA 注册 (T14)：link manifest.json + theme-color meta + apple-touch-icon + apple-mobile-web-app-capable/title。内联 script 在 load 事件注册 SW，updatefound 检测新版本并 confirm 刷新
- main.go 静态文件路由 (T14)：PWA 文件直供（manifest/sw/offline/icons），sw.js 设置 Cache-Control:no-cache+Service-Worker-Allowed:/。dist/assets 长期缓存（Vite hash 文件名）。NoRoute fallback：先检查 public 目录静态文件，再返回 dist/index.html 支持 SPA 路由
- generate-icons.js (T14)：sharp 读取 icon.svg 生成 5 个尺寸 PNG（192/512/180-apple/32/16），npm run 一次性执行
- 暗色模式 (T15)：tailwind.config.js 已配置 darkMode:'class'，useDarkMode composable 提供 isDark ref + toggleDarkMode 函数（localStorage 持久化+系统偏好检测），AppLayout 侧栏已有明暗切换按钮（月亮/太阳图标）。本次补全了所有缺失 dark: 变体的组件：AgentFlow（ECharts isDark 驱动颜色+watcher 重绘）、Toast（四类型通知+确认弹窗）、NotFound（全元素 dark 类）、Upload/Quiz/Cards/Chat（状态徽章/答题面板/高亮/按钮）。颜色方案统一：背景 dark:bg-gray-900/800，卡片 dark:bg-gray-800，内框 dark:bg-gray-700，边框 dark:border-gray-700/600，主文 dark:text-gray-100，次文 dark:text-gray-400，彩色徽章统一 dark:bg-{color}-900/30 dark:text-{color}-400
- 全局搜索 (T16)：GET /api/search?q=关键词，LIKE 模糊匹配 materials(title+content)、cards(concept+detail+tags+memory_tip)、quizzes(question)，每类最多 8 条，按 user_id 隔离。返回 SearchResultItem 数组（type/title/subtitle/material_id/material_title），关联材料标题批量查询避免 N+1。前端 GlobalSearch.vue 使用 Teleport 渲染到 body，Ctrl+K/Cmd+K 打开，Esc 关闭，300ms 防抖搜索，结果按类型分组（材料/卡片/练习题），每组带彩色图标（蓝/绿/琥珀），支持上下键导航+Enter 跳转。侧栏和移动端顶栏均有搜索入口按钮，侧栏显示 Ctrl+K 快捷键提示。点击结果跳转：material→/materials/:id，card→/cards，quiz→/quiz
- Quiz 练习报告 (T17)：全部答完后顶部出现"答题回顾/练习报告"双 Tab 切换按钮，最后一题答完 1.5s 后自动跳到报告视图。报告包含：4 卡片总分概览（正确率/总用时/答对/答错），ECharts 多边形雷达图（按难度 3 维度+题型 3 维度展示掌握率，至少 3 维度才渲染，linearGradient 渐变填充+tooltip），智能复习建议（6 维度分析：整体正确率<50%/<70%/>90%、难度维度<60%、题型维度<60%、答题速度<10s/题、错题数量≤3/>3），维度正确率进度条（绿≥80%/琥珀≥60%/红<60%），逐题详情列表（题号圆形红/绿+对错图标+题型标签+难度标签+用时+答案对比双栏+解析），底部"返回答题回顾"+"重新练习"按钮（resetQuiz 清除所有 _answerHistory）。radarChart 在 showReport/isDark/answeredCount watcher 中 nextTick 重建，onUnmounted dispose 清理。纯前端实现，无需后端改动
- 学习成就系统 (T18)：18 种成就分 5 类（学习/练习/复习/探索/特殊），UserAchievement 表存储解锁记录，成就定义为 Go 静态配置（AllAchievements 切片）。GET /api/achievements 端点一次性计算所有成就进度（computeAchievementStats 批量查询 materials/cards/quiz_attempts/chat_messages/llm_traces 统计），自动检测并解锁新成就（checkAndUnlock），返回含 progress/progress_pct/unlocked/unlocked_at 的完整列表。前端 Dashboard 成就展示区：顶部解锁进度条+百分比，最近解锁徽章（amber 胶囊标签含图标+名称+相对时间），6 个分类过滤按钮（全部/学习/练习/复习/探索/特殊），6 列响应式网格（grid-cols-2/3/4/6），已解锁卡片金色渐变背景+✓标记，未解锁卡片灰度+进度条+当前值/目标值。骨架屏含成就占位。特殊成就包括：满分通关（连续 3 题以上全对）、学霸潜质（正确率≥80% 且至少 10 题）、全能学者（触发全部 4 种 Agent）、夜猫子（0-4 点有学习记录）
- 路由切换动画 (T19)：App.vue 顶层 router-view 和 AppLayout.vue 内层 router-view 均改为 v-slot + Transition 包裹，mode="out-in" 保证旧页面先离场再入场新页面。fade-slide 过渡效果：0.25s ease，离场向左平移 20px + 淡出，入场从右平移 20px + 淡入。CSS 定义在 App.vue 全局 style 中，Login↔AppLayout 切换和 AppLayout 内部页面切换均有动画
- 键盘快捷键 (T20)：Chat.vue 注册 document keydown 监听，`/` 键在非输入框中时聚焦聊天输入（检查 tagName 排除 input/textarea/select/contentEditable），`Ctrl+Enter`/`Cmd+Enter` 发送消息。头部新增 kbd 快捷键提示行（/ 聚焦 · Enter 发送 · Ctrl+K 搜索），placeholder 同步更新。Toast.vue 全局 Esc 监听关闭 confirm 弹窗（handleEscKeydown 检查 confirmState.show）。Graph.vue 全局 Esc 监听关闭右键上下文菜单（handleGlobalKeydown 检查 contextMenu.visible）。GlobalSearch.vue 已有 Esc+Ctrl+K 处理保持不变
- Onboarding 引导 (T21)：useOnboarding.js 采用模块级单例 ref（isActive/currentStep），保证 AppLayout 和 OnboardingOverlay 共享状态。3 步骤：上传分析→知识卡片→AI 对话，每步通过 data-onboarding 属性定位侧栏导航项。首次登录检查 localStorage 'studyforge-onboarding-done' 键，不存在则 800ms 延迟后自动启动。OnboardingOverlay.vue 使用 box-shadow: 0 0 0 9999px 实现聚光灯挖孔效果（高亮区域 pointer-events 自动穿透到目标元素），四周填充 4 个 div 区域点击可跳过。工具提示卡片：渐变装饰条+步骤指示器（当前步长条/已完成短条/未完成短条）+彩色图标+描述+上一步/下一步/跳过/开始学习按钮。智能定位算法：优先 position 指定方向（right），空间不足则自动切换到 left/top/bottom，含边界检测。箭头 div 根据方向自动定位。支持键盘操作：Esc 跳过、← 上一步、→/Enter 下一步。500ms transition-all 实现聚光灯和卡片平滑移动。暗色模式全适配。侧栏底部新增 ? 帮助按钮调用 resetOnboarding() 可重新触发引导
- 材料卡片预览 (T22)：Upload.vue 材料列表每个卡片行增加 mouseenter/mouseleave 事件。250ms 防抖延迟后解析 analysis_data JSON，仅在 completed/partial 状态且有分析数据时触发。浮层通过 Teleport 渲染到 body，fixed 定位在卡片右侧（right+12px），右侧空间不足（left+384>innerWidth）自动翻转到左侧。底部溢出时上移（max top = innerHeight-420）。浮层内容：Ai 标识+标题栏，max-h-64 可滚动区域含摘要文本（line-clamp-4）、知识点列表（前 4 条+超出计数）、概念关系（前 3 条 source→target+超出计数）、重要性评估（emerald 卡片），底部提示跳转详情页。pointer-events-none 防止浮层干扰鼠标事件。Transition 动画 0.18s ease + translateX(-6px) 入场。scoped style 含 preview-enter/leave 过渡类和 custom-scroll 3px 细滚动条（亮灰/暗灰适配）。onUnmounted 清理 hoverTimer
- Agent 时间线 (T23)：AgentTimeline.vue 新组件，纯 HTML/CSS 甘特图式时间线（不依赖 ECharts）。每个 Agent 一行，水平条形按时间轴定位。analyzeStartTime（Date.now()时间戳）作为 t=0，WebSocket 收到 Agent 结果时记录 finishedAt=Date.now()，agentFinishTimes computed 映射 {Analyst/QuizMaster/CardMaker/MapBuilder → finishedAt}。条形 left=(finishAt-duration-startTime)/timeScale，width=duration/timeScale。running 态 Agent 条形随 200ms tickInterval 实时增长（endMs=now-startTime），带 shimmer 流光动画。智能时间轴刻度：≤10s→2s间隔，≤20s→5s，≤45s→10s，≤120s→15s，>120s→30s。bars 排序：完成的按时长升序在前，running 在后。颜色：蓝色运行中+shimmer，绿色完成，红色失败。Upload.vue agents 数组新增 finishedAt 字段，WebSocket 和 polling 两条路径均记录完成时间戳。AgentFlow.vue 新增 timelineStart/agentFinishTimes props 并嵌入 AgentTimeline
- 响应式布局 (T24)：全应用统一响应式 padding 策略 p-4 sm:p-6 lg:p-8。Upload.vue 材料列表在 md 断点切换布局：移动端（<768px）采用垂直卡片——上行标题+日期+状态徽章，下行操作按钮（查看/分析/删除）以 border-t 分隔和 bg-gray-50/800 背景突出；桌面端保持原有横排 flex。Dashboard.vue 材料列表同样 md 断点切换，移动端卡片内标题可点击跳转详情。Cards.vue 工具栏改为 flex-col sm:flex-row 垂直堆叠+flex-wrap 防溢出，搜索框 w-full sm:w-56 移动端全宽。MaterialDetail.vue 导航栏改 items-start 允许按钮不对齐，标题 text-xl sm:text-2xl，操作按钮 shrink-0 flex-wrap justify-end 自动换行。Quiz.vue 头部改 flex-col sm:flex-row，标题和描述文本响应式字号，完成横幅 flex-col sm:flex-row。Graph.vue 仅 padding 响应式。骨架屏同步适配响应式 padding 和列表布局。所有改动保持暗色模式 dark: 变体完整
- API 分页 (T25)：parsePagination(c *gin.Context) (int, int) 公共辅助函数定义在 material.go，解析 limit/offset 查询参数，默认 limit=20 offset=0，上限 200。5 个列表端点已加分页：GET /api/materials（默认 20）、GET /api/cards（默认 20，兼容 material_id/difficulty/due 过滤）、GET /api/quizzes（默认 20）、GET /api/conversations（默认 20，原生 SQL 加 LIMIT ? OFFSET ?）、GET /api/dashboard/traces（默认 50，上限 500）。响应格式统一从裸数组改为 `{data: [...], total: N, limit: N, offset: N}` 包装对象。每个端点在分页查询前先跑 Count 查询获取总数。前端所有消费者已更新：axios res.data 是 HTTP body（含 data 字段），故数组取 res.data.data。Cards.vue 和 Quiz.vue 因依赖客户端搜索/过滤功能，传 limit=200 加载全量数据。client.js 的 listMaterials 和 listConversations 从无参改为接受可选 params 对象
- IP 限流 (T26)：IPRateLimiter 纯标准库实现令牌桶算法（无外部依赖），sync.Mutex 线程安全。每个客户端 IP 维护独立 tokenBucket（tokens/maxTokens/refillRate/lastRefill）。allow() 方法按经过时间补充令牌（不超过 burst 上限），消费 1 个令牌成功则放行，否则返回 429。cleanStale() 每 5 分钟扫描清理 10 分钟无活动的 IP 桶防内存泄漏。两级限流：publicLimiter（5 req/s, burst 10）应用于 /api 公开路由组（login/register/health），authLimiter（20 req/s, burst 40）应用于认证路由组。WebSocket 端点 /ws 不经过限流中间件（长连接不适用）。前端收到 429 时显示"请求过于频繁，请稍后再试"
- Seed 演示数据 (T27)：POST /api/seed 一键生成 2 篇完整示例材料（"Go 并发编程入门"和"机器学习基础"），每篇含 Markdown 正文、analysis_data（AnalystOutput JSON: summary/key_points/relationships/importance）、graph_data（ECharts Graph 格式 nodes+edges），配套 9 张知识卡片（含 concept/detail/formula/memory_tip/difficulty/tags）和 8 道练习题（choice/fill/short_answer 三种题型+options/answer/explanation）。幂等检查：通过标题匹配判断是否已生成，重复调用返回 seeded=false。所有数据按 user_id 隔离
- 错误边界 (T28)：ErrorBoundary.vue 使用 Vue 3 onErrorCaptured 钩子捕获子组件渲染错误，error ref 存储错误状态，提供 reset() 重试功能。错误 UI：红色图标+"组件出错了"+错误消息+重试按钮，暗色模式适配。return false 阻止错误向上传播。App.vue 顶层 ErrorBoundary 包裹 router-view。main.js 注册 app.config.errorHandler 记录全局组件错误日志，window unhandledrejection 事件捕获未处理的 Promise 拒绝
- Vite 构建优化 (T29)：vite.config.js 新增 build.rollupOptions.output.manualChunks 函数式配置。node_modules 路径匹配策略：echarts+zrender→echarts 独立包（~800KB 体积最大的库单独拆出），marked→marked 包（Markdown 渲染库），vue/pinia/axios→vendor 包（核心运行时库合并）。各路由页面保持 Vite 默认 code-splitting（每个 lazy-loaded route 独立 chunk）。构建产物 13 个 JS chunk，实现缓存粒度优化：第三方库变更不影响业务代码 chunk hash
- API 文档 (T30)：ApiDocs.vue 纯前端 OpenAPI 风格接口文档页面，8 大分类（认证/材料/卡片/练习/对话/会话/其他）共 35+ 端点。每个端点含 HTTP 方法彩色标签（GET 蓝/POST 绿/PUT 琥珀/DELETE 红）、路径、描述、参数表格（名称/位置/类型/说明）、响应示例（monospace 代码块）、认证锁标记。顶部搜索框按路径/方法/描述/摘要模糊过滤。分类筛选按钮（pill toggle）支持单选切换。端点卡片可点击展开/收起详情。侧栏新增"API 文档"导航项（文档图标），路由 /api-docs。暗色模式全适配
- Handler 测试框架 (T31)：testutil.go 提供 5 个辅助函数——setupTestDB() 使用 SQLite :memory: 自动迁移全部 9 个模型；setupTestHandler() 创建最小化 Handler（仅 DB+JWT，不依赖 LLM/VectorStore）；setupTestRouter() 模拟 main.go 路由结构（公开路由+JWT 认证路由组）；createTestUser() 直接写入 bcrypt 加密用户；getAuthToken() 通过 Login 端点获取有效 Token；authRequest() 创建带 Bearer 头的请求。handler_test.go 16 个测试用例覆盖：HealthCheck 200+JSON body 验证、Login 4 场景（正确凭据/错误密码/不存在用户/缺字段）、Register 5 场景（成功+token/user 验证/重复用户名409/无效邮箱400/用户名过短/密码过短）、ListMaterials 3 场景（新用户空列表+分页 wrapper 结构/无 token 401/无效 token 401）、UploadMaterial 成功创建、Register→Login 集成流程、Register→ListMaterials 集成流程。所有测试使用 SQLite 内存模式，每个测试独立数据库实例，无测试间干扰。运行命令：E:\bin\go.exe test ./internal/handler/ -v -count=1（-count=1 禁用缓存确保每次执行）
- Model 层测试 (T32)：model_test.go 35 个测试用例。SM-2 间隔重复算法从 handler 提取到 Card.ApplyReview(result) 方法（card.go），handler 的 ReviewCard 改为调用 card.ApplyReview(req.Result)。ApplyReview 封装完整 SM-2 逻辑：ReviewCount++、mastered 路径（0→1→3→interval*easeFactor 递增，EaseFactor +=0.1 上限 3.0）、review 路径（interval 重置 1，EaseFactor -=0.2 下限 1.3）、NextReviewAt 计算。SM-2 测试覆盖 10 个场景：首次 mastered、二次 mastered、三次 mastered（int 截断验证）、EaseFactor 上限/已在上限、review 重置/EaseFactor 下限/已在下限、连续 4 次 mastered 全序列、review→mastered 恢复序列。验证测试使用 validator.New().SetTagName("binding") 匹配 Gin 的 binding 标签名（Gin 不用默认的 validate 标签）。UploadRequest 验证 4 场景：Title required、ContentType required、ContentType oneof=text url（非法值+两个合法值）。RegisterRequest 验证 8 场景含 min/max 长度和 email 格式。Achievement 完整性 5 个测试：AllAchievements 无重复 ID、每个含 Name/Description/Icon/Category/Target>0、Category 在合法类别中、数量≥18、AchievementCategories map 覆盖所有使用的类别。LLMTrace LatencyMs 转换测试 4 个延迟值（1s/500ms/2.5s/100ms）。运行命令：E:\bin\go.exe test ./internal/model/ -v -count=1
- RAG 向量存储测试 (T33)：retriever_test.go 38 个测试用例。两种 mock EmbeddingProvider：mockEmbeddingProvider（基于 MD5 hash 生成确定性归一化向量，支持 failOnText 注入错误）和 directionalEmbedder（根据文本关键词生成可控方向的归一化向量，用于精确测试余弦相似度排序：query→[1,0,...]、similar→[0.9,0.1,...]、medium→[0.5,0.5,...]、different→[0,...,1]）。ChunkDocument 测试 6 场景：空文档返回 nil、短文档单 chunk 内容不变、长文档多 chunk 索引递增+内容非空、单超长段落 splitBySize 切分+重叠检测、多短段落合并、纯空白返回空。UpsertPoints 测试 3 场景：写入后 Search 召回且完全匹配相似度≈1.0、chunks/vectors 数量不匹配返回错误、空 chunks 无错误。Search 测试 4 场景：directionalEmbedder 验证余弦相似度降序排列+similar 排第一、topK 截断（3/5/超量）、materialID 过滤（两个材料各 2-3 条分别过滤+空 materialID 全量）、空存储返回 0 结果。SemanticSearch 测试 2 场景：alice/bob 用户隔离（各 2 条+空 userID 返回全部 4 条）、embedding 失败返回错误。DeleteMaterialVectors 测试 2 场景：删除 mat-1 后 mat-2 不受影响+全局只剩 mat-2、删除不存在的材料不影响现有数据。cosineSimilarity 测试 6 场景：相同向量≈1.0、正交向量≈0、相反向量≈-1.0、不同长度返回 0、空向量返回 0、零向量返回 0（除零保护）。IndexMaterial 集成测试 3 场景：端到端（切分+向量化+存入+SemanticSearch 可召回）、空内容跳过、embedding 失败返回错误。VectorStore 基础功能 3 场景+Embedder 3 场景（确定性验证+空 chunks+provider 失败）+splitBySize 4 场景+splitParagraphs 3 场景+并发安全测试（10 goroutine 并发写入+10 goroutine 并发读取验证无 race condition）。运行命令：E:\bin\go.exe test ./internal/rag/ -v -count=1
- Memory 对话记忆测试 (T34)：memory_test.go 34 个测试用例。ConversationMemory 三层记忆架构（短期缓冲+长期摘要+RAG）全面测试。NewConversationMemory 4 场景：默认 bufferSize=10（传入 0 或负数）、自定义值、初始状态验证（buffer/summary/roundCount 全为空/零）。AddMessage 3 场景：单条消息验证 role+content、多条消息顺序保持、GetBufferText 包含新消息（用户/助手前缀）。滑动窗口 2 场景：bufferSize=3 时添加 4 轮（8条）后 buffer 长度=6（阈值=bufferSize*2），首条消息为第 2 轮的 Q；bufferSize=2 时精确验证 buffer[2:] 切片移除逻辑。GetShortTermMemory 深拷贝验证：修改返回切片不影响原始 buffer。NeedsSummary 参数化测试覆盖 12 个轮次值（0/1/5/9/10/11/15/19/20/21/100/101），仅在 roundCount>0 且 roundCount%10==0 时返回 true；模拟摘要后继续对话到 20 轮再次触发的完整流程。BuildContext 7 场景：全空返回空字符串、仅摘要（验证无 RAG/缓冲段）、仅 RAG、仅缓冲、三层完整+顺序验证（strings.Index 比较三段位置确保摘要<RAG<缓冲）、摘要+缓冲无 RAG、RAG+缓冲无摘要。并发安全 2 场景：10 goroutine 并发 AddMessage+5 goroutine 并发读（GetShortTermMemory/GetLongTermSummary/GetBufferText/BuildContext/NeedsSummary）+3 goroutine 并发 IncrementRound+UpdateSummary；100 次读写竞争测试。边界条件：bufferSize=1 极端滑动窗口、空内容消息保留。运行命令：E:\bin\go.exe test ./internal/memory/ -v -count=1
- Middleware 测试 (T35)：middleware_test.go 26 个测试函数（含 4 个子测试共 30 个用例）。JWT 中间件测试 8 场景：无 Authorization header 返回 401、格式错误 4 种子测试（无 Bearer 前缀/仅 Bearer/多余空格/空 Bearer）、过期 token 返回 401（exp 设为过去时间）、错误 secret 签名返回 401、缺少 user_id claim 返回 401、空 user_id 返回 401、有效 token 正确设置 userID+username 到 gin context 并返回 200、jwt.SigningMethodNone 签名方法被拒绝返回 401。GenerateToken+ValidateToken 4 场景：有效生成后验证可解析出 userID、expireHours=0 导致过期返回错误、错误 secret 返回错误、无效 token 字符串返回错误。RateLimit 中间件 4 场景：burst 内全部 200、超出 burst 返回 429（含 error 字段验证）、令牌恢复后重新 200（100 req/s 等待 20ms 恢复 1 令牌）、不同 IP 独立桶互不影响。tokenBucket 单元测试 2 场景：连续消费+补充验证（elapsed*refillRate 计算）、令牌上限 cap 到 maxTokens。cleanStale 3 场景：手动注入活跃(2min前)+陈旧(15min/20min前)桶验证仅清理旧桶、清理间隔未到时跳过执行、执行后 lastClean 更新为当前时间。NewIPRateLimiter 默认值验证（rate/burst/cleanup/buckets 初始空）。Allow 首次调用自动创建 IP 桶。并发安全测试：20 goroutine 同时请求同一 IP 验证无 panic/data race。测试使用 setupJWTRouter/setupRateLimitRouter 两个辅助函数搭建独立测试路由，makeToken 辅助函数生成自定义 JWT。运行命令：E:\bin\go.exe test ./internal/middleware/ -v -count=1
- 前端测试框架 (T36)：Vitest 4.1.8 + @vue/test-utils 2.4.11 + jsdom 29.1.1。配置写在 vite.config.js 的 test 字段（globals:true + environment:'jsdom' + include 匹配 src 下 *.spec.js/*.test.js）。package.json 新增 "test":"vitest run" 和 "test:watch":"vitest" 脚本。Toast.spec.js 14 个测试：Toast 通知渲染 7 场景（初始空/四种类型图标+CSS类验证/多 toast 并发/点击关闭按钮移除+300ms 延迟动画用 vi.useFakeTimers+vi.advanceTimersByTime/自动超时移除）、Confirm 弹窗 6 场景（初始不显示/Teleport 到 body 后显示+document.body.querySelector 查询/确定返回 true/取消返回 false/Esc 关闭）、暗色模式 CSS 类验证。ErrorBoundary.spec.js 11 个测试：正常渲染 3 场景（slot 组件/文本/多子元素）、错误捕获 5 场景（子组件 throw Error 后显示错误 UI+标题"组件出错了"+错误消息/重试按钮存在/SVG 图标存在/无 message 属性时显示默认文案"发生了未知错误"/onErrorCaptured return false 阻止冒泡）、重试功能 2 场景（首次报错→重试后恢复正常/持续报错仍然显示错误 UI）、console.error 日志验证。关键模式：Toast 的 Confirm 弹窗使用 <Teleport to="body">，测试需用 document.body.querySelector 而非 wrapper.find 查询 Teleport 内容；afterEach 用 wrapper.unmount() 正确清理 Teleport DOM 而非 document.body.innerHTML=''（后者会破坏 Vue Teleport 锚点导致 insertBefore 错误）；useToast/useConfirm 是模块级单例（reactive），beforeEach 手动 splice toasts 数组+重置 confirmState 保证测试隔离。运行命令：cd web && npx vitest run
- GitHub Actions CI/CD (T37)：.github/workflows/ci.yml 配置 3 个并行 Job。Go Job：actions/checkout@v4 → setup-go@v5（go-version:'1.24'+cache:true）→ apt-get install gcc（sqlite3 CGO 依赖）→ go build ./... → go test ./... -v -count=1 → go vet ./...。Vue Job：checkout → setup-node@v4（node-version:20+cache:npm+cache-dependency-path:web/package-lock.json）→ npm ci → npm run build → npm run test。Lint Job：checkout → setup-go → golangci/golangci-lint-action@v6（version:latest+args:--timeout=5m）。触发条件：push 到 main 分支 + pull_request 到 main 分支。README.md 包含 CI 状态徽章（GitHub Actions workflow badge）+ shields.io 徽章（Go 1.24/Vue 3.4/License MIT）+ 完整项目文档（功能特性 9 项、技术架构表、快速开始 4 步、Docker 部署、API 概览 15 端点、完整项目结构目录树、测试命令、CI/CD 说明、贡献指南）。README 中 CI badge URL 的 OWNER 占位符需替换为实际 GitHub 用户名
- 卡片滑动学习模式 (T38)：CardStudy.vue 全屏卡片学习页面，路由 /study，侧栏"学习模式"导航项（学术帽 SVG 图标，位于知识卡片和练习场之间）。数据从 listCards({limit:200}) 获取，按 due 优先排序（next_review_at 为空或已过期的排前面）。手势滑动：touchstart/touchmove/touchend 三事件监听在 onMounted 中以 {passive:false} 注册到 cardContainerRef，touchmove 中当 |dx|>|dy| 且 |dx|>10 时 preventDefault 阻止页面滚动。滑动阈值取 min(视口宽*25%, 120px)，超过阈值触发导航，否则回弹。滑动时卡片跟随手指位移+旋转（rotation = dx/innerWidth*12 度）。3D 翻转：perspective:1200px + transform-style:preserve-3d + backface-visibility:hidden，点击卡片（非滑动时，swipeOffset 绝对值<8px）切换 isFlipped 状态触发 rotateY(180deg) 过渡（0.6s cubic-bezier）。导航动画 doNav(direction)：先 350ms 将当前卡片滑出屏幕 → 关闭 transition → 设置新卡片从对侧入场位置 → void offsetWidth 强制 reflow → 重新启用 transition → 滑入中心。三按钮操作：「不熟」调用 reviewCard(id, 'review') 重置间隔+Toast 反馈+250ms 后自动导航；「模糊」不调 API 直接跳下一张+计数+Toast；「掌握」调用 reviewCard(id, 'mastered') 延长间隔。键盘快捷键：←/→ 导航（preventDefault）、空格翻转、1/2/3 对应三个按钮。完成页显示本轮统计（已掌握/不熟练/模糊跳过三项计数）+「再来一轮」按钮（reset+重新加载）+「返回卡片库」链接。卡片高度 clamp(320px, 55vh, 460px) 响应式。堆叠效果：后两张卡片用 scale(0.96)/translate-y-2/opacity-60 和 scale(0.92)/translate-y-4/opacity-30 装饰层。桌面端底部显示键盘快捷键提示（←/→ kbd 标签）。暗色模式全适配，背面渐变从 primary-50 到 indigo-50（暗色：gray-800 到 gray-700）。背面可滚动区域 custom-scroll 3px 细滚动条
- 跨材料知识图谱 (T39)：GetAllKnowledgeGraphs 端点 (GET /api/graph/all) 查询用户所有已完成且含 graph_data 的材料，合并为统一图谱。节点名添加材料标题前缀（最多6字+"·"）防止跨材料 ID 冲突。每个材料分配 10 色调色板颜色（materialColors 数组：indigo/pink/emerald/amber/blue/violet/red/cyan/lime/orange），节点携带 material_id/material_title/material_color 字段。边映射为前缀后的节点名。前端 Graph.vue：下拉框新增"全部材料 (跨材料图谱)"选项（value="all"），选择后调用 getAllGraphs。initChart 在 isAllMaterials 模式下用 node.material_color 替代 categoryColors 着色。跨材料关系高亮按钮（紫色 pill toggle）触发 highlightCrossMaterial：遍历所有边找出 source.material_id ≠ target.material_id 的跨材料边，紫色(#a855f7)加粗(3px)+opacity 1，同材料边 opacity 0.08，关联节点紫色边框+阴影，非关联 opacity 0.15。右上角图例在跨材料模式显示材料名+颜色圆点（标题超 8 字截断），单材料模式保持分类图例。节点点击在跨材料模式下 router.push(/materials/:material_id) 跳转详情页。tooltip 在跨材料模式显示"材料: xxx"。resetNodeStyles/applySearchHighlight/highlightNodeConnections 三个样式更新函数均已适配 isAllMaterials 条件分支。路由 /graph/all 注册在 /graph/:material_id 之前防止参数匹配冲突
- Agent 两阶段流水线 (P0-perf)：ProcessMaterial 从"4 Agent 并发读全文"改为两阶段流水线。Phase 1：Analyst 独享完整原文（不截断，零信息丢失），提取 summary/key_points/relationships/importance 结构化知识。Phase 2：buildCompactContext 将 AnalystOutput 转为精炼文本（【材料标题】+【摘要】+【核心知识点】编号列表含concept/difficulty/detail+【概念关系】from→to:type+【重要性】，通常~2000字 vs 原文可能 20000+字），CardMaker/QuizMaster/MapBuilder 三个 Agent 并发读取这份短上下文，input tokens 大幅减少，Phase 2 延迟从~37s 降至~8s。collectResult 闭包统一处理 Judge 评估+WebSocket OnResult 推送+DB 持久化+saveTrace，消除原 4 处重复代码。Analyst 失败时 Phase 2 降级使用 truncateContent 截断原文（8000字符，头 60%+尾 40%+段落边界断开+"[...中间内容已省略...]"标记）。truncateContent 使用 []rune 保证 UTF-8 完整性，lastIndex/indexByte 辅助函数在换行符处断开避免截断到句子中间。strings 包已导入（strings.Builder 用于 buildCompactContext）
- 渐进式提示系统 (T41)：Quiz 模型新增 hint_1/hint_2/hint_3 三个 text 字段（可选，由 QuizMasterAgent 生成时填充）。QuizAttempt 新增 hints_used 字段（int, default 0），AnswerRequest 新增 hints_used 字段供前端传递。GetQuizHint 端点 (GET /api/quizzes/:id/hint?level=1|2|3) 按 user_id 隔离查询，根据 level 返回对应级别及所有更低级别的提示内容，同时返回 available 对象标记哪些级别有非空内容。AnswerQuiz 端点答错时附带 hint_1（如存在）。前端 Quiz.vue：每道题维护 _hintLevel(0-3)/_hints/_hintLoading 三个状态。requestHint() 函数逐级调用 API 并更新状态。提示按钮文案随级别变化（需要提示？→再给一点提示→最后提示），展示区三级用不同色系（amber 方向提示/orange 关键线索/red 接近答案）。已答题回顾显示 hintsUsed 标记和 hintFromResponse 答错参考。报告页显示 totalHintsUsed 和 hintsUsedCount 统计条。resetQuiz 清理所有 hint 状态。submitAnswer 传递 hints_used 参数。提示不影响得分
- 学习路径规划 (T42)：PathPlannerAgent (pathplanner.go) 遵循现有 Agent 模式——4 返回值 (output, inputTokens, outputTokens, error)+ChatWithUsage 调用。pathPlannerPrompt 要求输出 ordered_steps 数组（每步含 title/description/estimated_minutes/material_ids/prerequisites/difficulty），4-12 步，遵循由浅入深原则。GetLearningPath 端点 (GET /api/learning-path) 查询用户所有已完成材料，buildPathSummary 解析每份材料的 analysis_data JSON 提取摘要+核心知识点（前 8 条）+概念关系（前 5 条）构建精炼上下文。Agent 返回后经 materialMap 补充材料标题和卡片统计（card_count/reviewed_count），按 user_id 隔离查询。savePathTrace 记录 LLM 调用追踪（model name 从 h.LLM.ModelName 获取）。前端 LearningPath.vue：竖向时间线设计，左侧圆形节点按钮（序号/完成✓）+连接线颜色随复习进度变化（绿=完成/灰=未完成）。步骤卡片可点击展开/收起：展开显示完整描述+进度条+关联材料列表（router-link 跳转详情页）+操作按钮（查看材料/开始学习/查看卡片）。难度徽章三色（easy 绿/medium 琥珀/hard 红），预估时长标签，卡片复习计数标签，前置依赖提示（amber 色+步骤标题拼接）。顶部路径概览卡片（渐变背景+步骤数/总分钟/材料数+概述文本）。总体进度条在底部（渐变色从 primary 到 indigo）。全部完成时显示绿色庆祝横幅。空状态引导去上传页。骨架屏 4 步占位。暗色模式全适配。响应式 p-4 sm:p-6 lg:p-8 + max-w-4xl 居中。侧栏新增"学习路径"导航项（路线图标 SVG，位于知识图谱和 AI 对话之间）。路由 /learning-path 注册在 /graph 和 /chat 之间
- Quiz 错题本 (T43)：QuizMistake 模型 (mistake.go) 含 user_id/quiz_id/user_answer/correct_answer/mistake_at/reviewed 字段，Quiz 关联字段用 gorm:"-" 避免外键约束。AnswerQuiz (study.go) 答错时自动 upsert：首次答错创建记录，重复答错更新答案+时间+重置 reviewed=false（确保重新答错的题目回到待复习状态）。ListMistakes 使用 MistakeItem 扁平响应结构（非嵌套 Quiz 对象），批量查询 Quiz 详情+材料标题避免 N+1（quizMap+materialTitles 两个 map 分别缓存）。GetMistakeStats 使用 JOIN quizzes ON quiz_mistakes.quiz_id 跨表聚合按 difficulty 分组统计。RetryMistakes 选取 reviewed=false 的最近 20 道错题，响应含 last_wrong_answer（上次错误答案供参考）和 mistake_id（重练答对时自动调用 reviewMistake 标记已复习）。路由注册注意 /mistakes/retry 必须在 /mistakes/:id 之前，否则 retry 会被当作 :id 参数匹配。前端 Mistakes.vue 双模式设计：列表模式（统计卡片+过滤标签+错题卡片+批量操作+分页）和重练模式（进度条+逐题作答+实时反馈+自动标记复习）。选择题选项使用 optionClass 函数同时标记正确答案（绿色）和用户错误答案（红色+line-through）。非选择题使用红绿双栏对比展示。重练模式下答对自动调用 autoReviewMistake 标记对应 mistake_id 为已复习。侧栏导航"错题本"位于练习场和知识图谱之间。路由 /mistakes 已在 router/index.js 注册
- 知识图谱关系修复：MapBuilder prompt 要求必须复用 Analyst 在「概念关系」部分提取的关系作为边（不遗漏），可补充额外关系。GraphNode.Category 类型从 int 改为 string（中文分类标签：核心概念/原理/算法/应用/模型/方法/公式/功能），与前端 categoryColors 的 key 匹配，节点按分类着色。Analyst prompt 关系 type 从英文 prerequisite/related/analogy 改为中文 8 种（依赖/前置条件/包含/关联/基于/对比/推导/实例化），要求至少 8 条关系。前端 Graph.vue 边渲染新增 label 显示（tooltip 中高亮显示关系类型+连线上方显示标签文字），边样式区分（对比/关联用虚线，其他实线），highlightNodeConnections 高亮时显示关联边标签。demo graph 已更新为含边标签+字符串分类
- 学科感知出题：QuizMaster prompt 新增「第一步：学科判断」，要求先分析材料属于 5 类学科（理工科/计算机/文科社科/医学生物/语言），据此调整出题策略。解答题 6 类标注适用学科（计算题理工科必出/证明题数学物理必出/算法设计题计算机必出/论述评价题文科必出/实验机制题医学必出/综合分析题所有学科适用）。tools.go executeCreateQuiz prompt 同步学科感知指令
- KaTeX 数学渲染：useMathRender.js composable 提供 renderMath(text) 和 renderMathWithHighlight(text, escapeAndHighlight) 两个函数。支持 4 种定界符（$$..$$ 和 \[..\] 为块级，$..$ 和 \(..\) 为行内）。renderMathWithHighlight 使用占位符策略：先提取数学公式替换为 \x00MATH{idx}\x00 占位符→对文本部分 HTML 转义+搜索高亮→恢复 KaTeX 渲染后的 HTML。避免 HTML 转义破坏 KaTeX 输出。所有视图（Cards/CardStudy/MaterialDetail/Chat/Quiz）已统一使用此 composable
- Anki 导出美化：ExportCards 改为 #html:true，Front 含概念名+难度徽章（绿 easy/琥珀 medium/红 hard），Back 三结构化区块（详解灰底/公式蓝底#f0f9ff/记忆技巧琥珀底#fffbeb），使用内联 CSS（Anki 不支持 style 标签但支持 inline style）。htmlEsc 函数转义 HTML 特殊字符，htmlNL 将换行转为 br 标签
- checkAnswer 函数新增 quizType 参数（string），当 quizType=="judge" 时对 userAnswer 和 correctAnswer 进行归一化（正确/对/T/TRUE/√/YES → "正确"，错误/错/F/FALSE/×/NO → "错误"），支持多种判断题输入格式
- 材料批量操作 (T44)：BatchAnalyzeMaterials (POST /api/materials/batch-analyze) 接受 ids 数组（最多 20 个），验证 user_id 所有权后过滤掉已在分析中的材料（activeAnalyses sync.Map 并发守卫复用）。有效材料状态更新为 analyzing 并初始化 analysisProgress 追踪。goroutine 内使用信号量（chan struct{} cap=2）限制最多 2 个并发分析，每个材料完成后通过 WebSocket 推送 batch_analyze_progress 事件（含 material_id/title/status），全部完成后推送 batch_analyze_complete 事件。BatchDeleteMaterials (DELETE /api/materials/batch) 接受 ids 数组（最多 50 个），逐个在事务内级联删除（Material+Card+Quiz）+清理 RAG 向量，失败的 ID 收集到 failedIDs 数组返回。路由注册在 :id 参数路由之前避免 gin 参数匹配冲突。前端 Upload.vue：selectedIds 使用 ref(new Set()) 保持响应式（Vue 对 Set 的变更检测需要替换整个 Set 引用）。每行 checkbox 在移动端和桌面端分别渲染（移动端左侧+桌面端 flex 行内）。全选 checkbox 支持 indeterminate 半选状态。批量操作栏使用 Transition name="batch-bar" 从上方滑入（0.25s ease）。WebSocket onmessage 新增 batch_analyze_progress（completed 计数累加+本地状态更新）和 batch_analyze_complete（重置状态+清空选择+刷新材料列表+Toast 成功提示）两个事件处理。批量删除使用 api.delete('/materials/batch', { data: { ids } }) 传 body（axios DELETE 请求体需用 data 字段包裹）
- 学习日历热力图 (T45)：GetCalendarHeatmap 端点 (GET /api/stats/calendar?year=2026) 查询 4 个活动源聚合每日学习计数。聊天消息通过 JOIN conversations 表关联 user_id（chat_messages 无直接 user_id 列）。练习题作答查 quiz_attempts.created_at。卡片复习查 cards.last_reviewed_at（IS NOT NULL）。材料上传查 materials.created_at。4 个查询结果按 DATE() 分组后合并到 activityMap（map[string]int64），然后生成全年 365/366 天零填充数据。level 分级：0=无活动，1=1-2次，2=3-5次，3=6-9次，4=10+次。前端 Dashboard.vue 替换原 30 天 heatmap 为 ECharts calendar 坐标系统（coordinateSystem:'calendar'），GitHub 贡献图风格绿色渐变（亮色 #ebedf0→#216e39，暗色 #1e293b→#39d353）。visualMap 使用 piecewise 类型 5 段分色。calendar 配置：cellSize ['auto',14]，周一首日(firstDay:1)，中文月份/星期标签，水平方向。tooltip 显示完整日期+星期几+活动次数。年份选择器支持切换近 4 年（当前年~当前年-3），切换时重新调用 loadCalendarData 获取数据并 dispose+重建图表。统计摘要 computed（活跃天数/总活动次数/连续活跃天数 streak），streak 从今天往前遍历 data 数组计算。isDark watcher 在暗色模式切换时自动重绘热力图（dispose+init 重建配色）。底部图例条（少→多 5 色方块）。移动端 sm 断点以下显示统计摘要在图表上方，桌面端显示在标题行右侧
- 知识图谱布局优化 (OPT-1)：Graph.vue 力导向参数调优——repulsion 300→800 增大节点间距避免重叠，edgeLength [80,200]→[120,250] 更均匀的边长范围，gravity 0.1→0.15 稍强中心引力防止图谱散落，新增 friction 0.2 加速布局收敛（ECharts 默认 0.6 会导致长时间抖动）。新增 edgeSymbol ['none','arrow'] 和 edgeSymbolSize [0,8] 为边添加方向箭头。边标签从始终显示改为默认隐藏、hover(emphasis)时显示——减少视觉杂乱，hover 时 emphasis.edgeLabel.show=true 和 emphasis.label.show=true 自动显示关联边和节点的标签。小节点标签条件显示——label.show 从 true 改为 n.symbolSize>=40，只有重要节点默认显示名称。resetNodeStyles 函数同步更新以保持搜索/高亮重置后的一致性
- 学习路径加载修复 (OPT-2)：LearningPath.vue 三项修复——(1) 15s 超时安全网：模块级 loadingTimer 变量，onMounted 中 generatePath() 之后 setTimeout 15000ms，超时后自动 loading=false+设置错误提示，finally 块中 clearTimeout 清理防止重复触发；(2) 骨架屏底部添加 animate-pulse 提示文字"AI 正在分析你的学习材料并规划最优路径，请稍候..."让用户知道正在加载而非卡死；(3) 空状态条件修复——原来要求 steps.length===0 && material_count===0 两者同时成立才显示空状态（&& 逻辑），改为 material_count===0 || (!ordered_steps || ordered_steps.length===0)（|| 逻辑），确保无材料时立即显示引导页，也处理 LLM 返回空步骤但材料存在的边界情况
- 卡片切换速度优化 (OPT-3)：CardStudy.vue 5 项动画时间缩减——flip-card-inner CSS transition 从 0.6s 减至 0.3s（3D 翻转动画），cardStyle computed 中 slide transition 从 0.35s 减至 0.18s（滑动位移动画），doNav 函数两阶段 setTimeout 从 360ms+360ms 减至 180ms+180ms（出屏+入屏动画），复习按钮提交后延迟从 250ms 减至 120ms。总复习→下一张感知时间从约 970ms 降至约 480ms（提速约 51%）。所有 cubic-bezier 缓动函数保持不变，仅修改 duration 值
- Quiz 难度与题型优化 (OPT-4)：quizmaster.go 系统提示三项增强——(1) 强制难度分布：easy 约 25%(2-4道)/medium 约 40%(4-8道)/hard 约 35%(3-6道)，明确禁止全 easy/medium 且 hard≥3道；(2) 数学计算 vs 工程应用区分指令：理工科解答题中两种各占约 50%，数学计算题要求精确数值+完整解析过程，工程应用题要求具体工程场景+参数代入+带单位结论（含 RLC 电路等示例）；(3) 解答题 difficulty 从"应主要为 medium/hard"改为"必须为 medium/hard，严禁 easy，hard≥2道"。tools.go executeCreateQuiz 两项改进——(1) 精简用户消息：删除与系统提示重复的学科判断/题型说明/解答题要求等内容，仅传知识点+难度偏好+数量请求，避免 LLM 收到矛盾指令；(2) 后置校验：LLM 返回后校验 difficulty(easy/medium/hard)和 type(choice/fill/judge/short_answer)合法性，非法值回退默认值，解答题 easy 自动提升为 medium
- 代码块增强 (T46)：Chat.vue 自定义 marked.Renderer.code 方法，围栏代码块渲染为 div.code-block-wrapper > span.code-lang + button.code-copy-btn + pre > code 结构。langLabels 映射 40+ 语言标识及别名（js→JavaScript, py→Python, ts→TypeScript 等），未知语言显示首字母大写。复制按钮使用 data-copy-code 属性存储 encodeURIComponent 编码的代码内容（避免 HTML 特殊字符破坏属性）。DOMPurify 从废弃的 ALLOWED_TAGS/ALLOWED_ATTR 迁移到现代 allowlist + ADD_ATTR API，显式允许 button/svg/rect/path 标签和 data-copy-code 属性。复制采用事件委托模式（@click="handleCopyCode" 绑定在 messagesContainer 上），通过 e.target.closest('[data-copy-code]') 匹配按钮，避免内联 onclick 被 DOMPurify 移除。navigator.clipboard.writeText 为主复制方式，失败时 fallback 到 document.execCommand('copy') + textarea 方案。复制成功后按钮文字变"已复制"+SVG 切换为 checkmark 图标+copied CSS 类（绿色边框/背景），2 秒后自动恢复。暗色模式 5 组 :deep() 样式覆盖：wrapper 边框(gray-700)、语言标签(gray-600)、复制按钮(gray-600→hover 变亮)、pre 背景(slate-900 #0f172a)、行内 code(bg-gray-700 text-fuchsia-400)。pre 覆盖 Tailwind prose 默认配色为 slate-800(#1e293b)，padding-top 2rem 为语言标签和复制按钮留空间。4px 自定义滚动条（slate-600 thumb+hover slate-500）
- 材料标签系统 (T47)：Material 模型 tags 字段为逗号分隔字符串（如"编程,Go,并发"），存储在 VARCHAR(500) 列中。前端上传时发送逗号分隔字符串（非数组），后端直接存储。GetTags 端点遍历用户所有材料的 tags 字段，按逗号拆分后计数去重，返回 [{name, count}] 按 count 降序。ListMaterials tag 过滤使用 LIKE '%tag%' 模糊匹配（简单但可能有子串误匹配，如"Go"匹配"Golang"，适合标签数量不多的场景）。Upload.vue 标签输入增强：focus 时显示已有标签建议下拉（filteredExistingTags 排除已输入的+按输入文字过滤），点击建议自动追加到输入框。parsedFormTags 将输入框内容拆分为 pill 标签可单独删除。标签过滤栏使用 pill toggle 单选模式（activeTagFilter ref），切换时 watch 自动重新加载材料列表。Cards.vue 和 Quiz.vue 使用 listMaterials({limit:200}) 加载材料列表构建 materialTagMap（material_id → [tag1,tag2]），在卡片正面和题目头部以 indigo 色小标签显示材料标签，与卡片/题目自身的标签用竖线分隔区分。GlobalSearch 后端新增 tags LIKE 匹配+subtitle 显示标签信息
- 通知系统 (T48)：Notification 模型含 type 三种类型（review_reminder/analysis_complete/achievement_unlocked），action_url 用于前端点击跳转。惰性生成策略：每次 GET /api/notifications 时调用 generatePendingNotifications 检查三个数据源（待复习卡片数/1小时内完成分析的材料/1小时内解锁的成就），按需生成通知并去重（复习提醒每日一条、分析完成按材料去重、成就按 achievement_id 去重）。CreateNotification 为公共方法，其他 handler 可直接调用。路由注意：/notifications/unread-count 和 /notifications/read-all 注册在 /notifications 之前避免 gin 路径匹配冲突。前端 AppLayout.vue：侧栏和移动端双入口铃铛按钮（含红色未读计数徽章），Teleport 面板含类型图标三色区分（amber 复习/green 分析/purple 成就）、未读蓝点、相对时间（刚刚/X分钟前/X小时前/X天前）、点击标记已读+跳转 action_url。60s setInterval 轮询 unreadCount，onUnmounted 清理定时器。Esc 关闭面板。notif-fade 过渡动画（opacity+translateY）
- OOM 崩溃修复 (OPT-5)：两项修复解决浏览器内存溢出。(1) 后端 ListMaterials (material.go) 新增 Select 子句排除 content/analysis_data/graph_data 三大 TEXT 字段——这些字段每份材料可达数百 KB 到数 MB，20 条材料同时加载会导致前端内存暴涨。Select 仅保留 id/user_id/title/content_type/source_url/status/tags/analyzed_at/created_at/updated_at 列表必要字段。(2) 前端 Upload.vue WebSocket 生命周期管理——connectWebSocket 开头先关闭旧连接（ws.onclose=null+ws.close()+ws=null）防止孤儿连接；analysis_complete/batch_analyze_complete/error 三个事件处理中主动关闭 WebSocket 释放资源。模块级 ws 变量确保每次只有一个活跃连接
- 知识图谱方框化 (OPT-6)：Graph.vue 将节点从圆形(circle)改为圆角矩形(roundRect)。symbolSize 从固定数值改为按文字长度动态计算 [Math.max(60, n.name.length*14+20), Math.max(36, (n.symbolSize||30)*0.8)]，确保长名称不被截断。label 始终显示白字加粗(overflow:'truncate')，itemStyle 加 borderRadius:6+borderWidth:1。力导向参数进一步调优：repulsion 800→1200（方框比圆形占更多空间需要更大斥力），edgeLength [120,250]→[150,300]，gravity 0.15→0.12。resetNodeStyles/applySearchHighlight/highlightNodeConnections/highlightCrossMaterial 四个样式更新函数全部同步 roundRect+borderRadius 样式。节点颜色在跨材料模式使用 material_color，单材料模式使用 categoryColors
- 知识卡片交互重构 (OPT-7)：Cards.vue 从"单击翻转"改为"单击放大+双击翻转"。使用 250ms setTimeout 区分单击/双击——单击延迟后打开 Teleport 模态框（enlargedCard ref），双击立即取消定时器并原地翻转（card._flipped）。模态框：全屏半透明遮罩+max-w-lg 520px 高圆角大卡片，正面显示完整内容（概念名+难度徽章+详情+公式+记忆技巧），背面显示复习按钮。支持 ESC 关闭+点击遮罩关闭+卡片内点击翻转。modal-fade 过渡动画。副标题改为"单击放大 · 双击翻转查看详情"
- 卡片切换速度二次优化 (OPT-8)：在 OPT-3 基础上进一步压缩 CardStudy.vue 动画时间——flip-card-inner transition 0.3s→0.2s（3D 翻转），cardStyle slide transition 0.18s→0.1s（滑动位移），doNav 两阶段 setTimeout 180+180→100+100（出屏+入屏），复习后延迟 120→60ms。总复习→下一张感知时间从 ~480ms 降至 ~260ms（提速约 46%）。所有 cubic-bezier 缓动函数保持不变
- 练习题三级提示生成修复 (OPT-9)：修复提示系统始终显示"此题暂无提示"的问题，涉及 4 个断点。(1) quizmaster.go：QuizItem 结构体新增 Hint1/Hint2/Hint3 json tag 字段，quizMasterPrompt JSON schema 新增 hint_1/hint_2/hint_3 输出要求，新增详细的三级提示生成要求段落（方向提示→关键线索→接近答案，递进式透露信息）。(2) orchestrator.go saveQuizzes：映射 item.Hint1/Hint2/Hint3 到 model.Quiz 的 Hint1/Hint2/Hint3 字段。(3) tools.go executeCreateQuiz：后置映射 q.Hint1/Hint2/Hint3，prompt 新增三级提示要求行。注意：已有数据库中的练习题不会有提示，需重新分析材料或生成新题目才能获得 hint 数据
- 答题后取消自动跳转 (OPT-10)：Quiz.vue submitAnswer 函数移除 1500ms 自动跳转下一题的 setTimeout。原来答题后 1.5 秒自动跳到下一未答题，用户来不及阅读解析。现在改为纯手动模式——用户点击"下一题"按钮才跳转。保留全部答完自动显示报告的逻辑（2000ms 延迟），让用户看完最后一题的解析后再查看报告
- 知识图谱关系标签常驻显示 (OPT-11)：Graph.vue 边标签从 hover 才显示改为始终可见。label.show 从 false 改为 !!e.label（有标签即显示），字号 11px 加粗，颜色使用紫色主题色（亮色 #6366f1/暗色 #c4b5fd），backgroundColor 半透明背景圆角 pill（亮色 rgba(255,255,255,0.9)/暗色 rgba(30,41,59,0.85)）提升可读性。连线加粗至 2px+opacity 0.9+curveness 0.2。对比/关联关系使用 dashed 虚线，其他使用 solid 实线。resetNodeStyles 和 highlightCrossMaterial 函数同步更新边标签样式
- Quiz 难度分布调优 (OPT-12)：quizmaster.go 系统提示难度分布从 easy 25%/medium 40%/hard 35% 调整为 easy 30%/medium 40%/hard 30%（easy 3-5 道/medium 4-8 道/hard 3-5 道），分布更均衡，避免挑战题占比过高影响学习体验。tools.go executeCreateQuiz 中指定难度偏好占比从 50% 提升至 60%，当用户明确指定难度时该难度的题目更多，其余为相邻难度
- 卡片书签与笔记 (T49)：Card 模型新增 is_bookmarked (bool, default:false) 和 user_note (text) 两个字段，GORM 自动迁移添加列。ToggleBookmark (PUT /cards/:id/bookmark) 切换书签状态，UpdateCardNote (PUT /cards/:id/note) 更新笔记内容（NoteRequest 结构体含 note 字段，可为空以清除）。ListCards 新增 bookmarked=true 查询参数过滤已书签卡片。ExportCards CSV 从 3 列扩展为 4 列（Front/Back/Notes/Tags），#tags column:4 指令告知 Anki 标签在第 4 列。前端书签按钮使用 amber 色系（已书签 amber-500/未书签 gray-300），@click.stop 防止触发卡片点击。笔记使用 800ms 防抖自动保存（saveCardNote 函数+模块级 noteSaveTimer），避免频繁请求。模态框 enlargedCard 与 cards 数组共享对象引用，书签状态自动同步。书签过滤使用客户端 computed 过滤（filter.bookmarked ref），与 due 过滤和搜索可叠加。暗色模式全适配（书签按钮 dark:text-gray-600/dark:hover:text-amber-500，笔记 textarea dark:bg-gray-700/dark:border-gray-600）
- 错题巩固强化 (Consolidate Practice)：新增 POST /api/mistakes/consolidate 端点（ConsolidatePractice handler），根据用户错题模式智能推荐同类练习题进行强化训练。算法流程：(1) 查询用户所有错题，单次遍历收集 materialID/type/difficulty 分布到 matStatsMap（每材料含 count+types+diffs 三个维度的 map）(2) sort.Slice 按材料错题数降序排列薄弱材料（替代 O(n²) 冒泡）(3) 单次批量查询 `material_id IN ? AND id NOT IN ?` 从所有薄弱材料中取候选题，Go 层按材料分组 + rand.Shuffle 随机打乱（替代逐材料 ORDER BY RANDOM() 全表扫描），按薄弱度顺序取题 (4) 不够则扩展到同题型+同难度的跨材料题目（remaining*5 cap 200 候选池 shuffle）(5) 三级 fallback：仍未找到则放宽限制允许复用已答过的题（仅排除错题本身）(6) 最多返回 15 道题。性能优化：DB 查询从 5+M+F 次降至固定 5-6 次（M=材料数），消除 N+1 材料标题查询（合并错题材料+巩固题材料为单次 `WHERE id IN ?`），薄弱点分析使用预计算 matStatsMap 零额外 DB 查询。响应含 quizzes（ConsolidateItem）、weak_areas（WeakArea，从 matStatsMap 直接取 main_type/main_difficulty）、mistake_stats。前端 Mistakes.vue 新增 violet 色系"巩固强化"按钮+完整练习模式 UI：薄弱点分析卡片面板（grid 3 列）、巩固进度条、选择/填空/简答三题型支持、答题反馈+解析、完成总结。v-if/v-else-if/v-else 三态切换（重练/巩固/列表）。错误处理区分 404（"请重启后端服务"）/500（显示具体错误）/其他，console.error 输出调试信息。API client 超时 60s。所有 DB 查询均带 user_id 隔离
- 材料导入增强 (T50)：后端 fileparser.go 已支持 .pdf/.docx/.md/.txt/.markdown 五种格式。后端使用 github.com/nguyenthenguyen/docx 解析 DOCX（docx.ReadDocxFile+Editable+GetContent），.md/.txt/.markdown 通过 io.ReadAll+io.LimitReader(50MB) 直接读取。前端 client.js uploadFile 新增可选 onProgress 回调参数，通过 axios onUploadProgress 配置监听上传进度（progressEvent.loaded/progressEvent.total*100 取整百分比）。Upload.vue 新增 uploadProgress ref（0-100 百分比）和 fileSizeWarning ref（>10MB 文件警告文本）。processFile 函数在上传前检查 file.size>10*1024*1024 并设置警告提示（amber 色三角形警告图标+文本），上传过程中传递进度回调实时更新百分比。上传进度条 UI：蓝色渐变条（h-1.5 rounded-full bg-blue-500），初始无进度时显示 15%+animate-pulse 提示，有进度后平滑过渡到实际百分比。文件类型图标从 emoji 替换为 SVG 图标：PDF（红底 #FEE2E2+#EF4444 stroke+"PDF"标签）、Word/DOCX（蓝底 #DBEAFE+#3B82F6 stroke+"DOCX"标签）、Markdown/MD（绿底 #D1FAE5+#10B981 stroke+"MD"标签）、TXT（灰底 #F3F4F6+#9CA3AF stroke+横线模拟文本行），40x40 viewBox，圆角矩形文件造型。clearFile 函数同步清除 uploadProgress+fileSizeWarning。暗色模式进度条使用 dark:bg-blue-400/dark:bg-gray-700
- 多 Agent 辩论模式 (T51)：DebateOrchestrator (debate.go) 实现三轮顺序辩论+综合总结。辩论流程：Round 1 Analyst 从概念本质角度分析（无前置上下文）→ Round 2 QuizMaster 从出题者/考试角度评论（user message 包含 Round 1 分析师观点）→ Round 3 CardMaker 从记忆策略角度总结（user message 包含 Round 1+2 两位专家观点）→ Round 4 Summary 综合总结（包含全部三轮发言）。每轮使用 ChatWithUsage 4 返回值（content, inputTokens, outputTokens, error）。角色系统提示为 const 字符串，不含格式化占位符（避免 Sprintf 开销），辩论主题和前轮上下文通过 user message 传递。总结失败时降级为固定文本摘要（不阻塞整体返回）。DebateMessage 结构含 agent_name/role/avatar/color/round/content/duration_ms，前端根据 agent_name 映射颜色配置（Analyst 蓝/QuizMaster 琥珀/CardMaker 翠绿/Summary 紫）。前端 Debate.vue 使用 marked+DOMPurify 渲染 Markdown 内容，收到全部消息后逐条 300ms 间隔展开（async for+setTimeout 延迟），配合 message-in CSS 入场动画（translateY+opacity 0.4s）。加载状态通过 simulateRoundProgress 每 500ms 推断当前轮次（基于已收到消息数量），更新 loadingLabel/loadingRole/loadingHint。空状态提供 8 个推荐主题 pill 按钮（TCP 三次握手/二叉搜索树/牛顿第三定律等），点击直接发起辩论。API 超时设为 120s（辩论涉及 4 次 LLM 调用，总耗时可能 60-90s）。StartDebate handler 的 DebateRequest 使用 binding:"required,min=1,max=500" 验证 concept 字段。路由 POST /debate 注册在认证路由组中，按 user_id 记录 LLMTrace（agent_name="Debate"）。侧栏导航"Agent 辩论"使用双气泡对话 SVG 图标，位于学习路径和 AI 对话之间
- Agent 质量评分可视化 (T52)：LLMTrace 模型新增 judge_comment (VARCHAR 1000) 字段持久化 Judge 评语文本。AgentResult 新增 JudgeComment 字段，collectResult 闭包中 Judge.Evaluate 成功后同时保存 evalResult.Overall 到 QualityScore 和 evalResult.Feedback 到 JudgeComment，日志输出含三维度分数（相关性/准确性/完整性）。saveTrace 将 JudgeComment 传播到 LLMTrace。GetMetrics 端点扩展两个新字段：quality_distribution（SQL CASE WHEN 按 0-3/3-6/6-8/8-10 四区间 GROUP BY 统计，确保所有区间存在）和 judge_comments（查询 judge_comment 非空且 quality_score>0 的最近 20 条记录，按 agent_name 分组每组最多 3 条）。前端 AgentFlow.vue 质量评分从纯文本改为 5 星 SVG 可视化（10 分制映射 5 星，starClass 函数支持满星/半星/空星三态，amber 色系+暗色模式适配），hover 显示 Judge 评语 tooltip（group-hover/score CSS 触发，absolute 定位在评分区域上方，含 info 图标+标题+评语文本+三角箭头指示器）。Upload.vue agents 数组新增 judgeComment 字段，WebSocket agent_output 事件中提取 result.judge_comment 传递。Dashboard.vue 质量评分区域增强：仪表盘高度从 200px 降至 160px 为分布条和评语腾出空间，新增 4 色分段质量分布条（红 0-3/琥珀 3-6/绿 6-8/靛蓝 8-10，flex 布局按百分比分配宽度），分布图例条，最近评语列表（最多 5 条，按时间倒序从 commentsByAgent 扁平化+排序，每条含 Agent 中文标签+分数徽章+评语文本，max-h-36 可滚动+custom-scroll 3px 细滚动条）。柱状图 tooltip 自定义 formatter：显示 Agent 中文名+质量分/延迟数值+最近一条 Judge 评语（分割线分隔+暗色模式颜色适配）
- 自适应出题 (T53)：analyzeUserLevel 辅助函数查询用户最近 20 次 quiz_attempts（按 created_at DESC），计算正确率后分三档推荐：>80% → medium/hard（高水平挑战），<50% → easy/medium（基础巩固），50-80% → 全部难度（均衡练习）。无答题记录时返回全部难度。GetDifficultyLevel 端点 (GET /api/quizzes/difficulty-level) 返回 accuracy、total_attempts、recommended_difficulty 数组、reason 文本，以及 difficulty_stats（JOIN quizzes 表按 difficulty GROUP BY 统计各难度答题数和正确数）和 quiz_counts（各难度题目总数）。ListQuizzes 新增 recommended=true 查询参数，启用时调用 analyzeUserLevel 获取推荐难度后用 WHERE difficulty IN ? 过滤，响应额外包含 recommended/recommended_difficulty/recommended_reason 字段。路由注册注意 /quizzes/difficulty-level 必须在 /quizzes/:id 之前。前端 Quiz.vue 新增 studyMode ref（'all'/'recommended'），switchMode 函数重置所有状态（correctCount/wrongCount/currentIndex/selectedOption 等）后重新加载数据，推荐模式额外调用 loadDifficultyInfo 获取难度信息。diffBadgeClass 辅助函数为不同难度返回对应颜色 class。推荐模式横幅展示：渐变背景+正确率圆环（颜色按正确率变化）+已答题数+推荐难度徽章+各难度答题统计条。submitAnswer 中在推荐模式下每次答题后异步调用 loadDifficultyInfo 刷新难度推荐（不阻塞 UI）。空状态区分推荐模式（提示切换全部题目）和全部模式（提示上传材料）。middleware_test.go makeToken 辅助函数自动补全 iss/aud claims 修复 S14 安全修复后导致的 TestJWT_ValidToken_SetsContext 测试失败
- 环境配置校验 (T54)：config.go LoadConfig() 三层优先级——环境变量 > config.yaml > defaultConfig() 内置默认值。支持 CONFIG_PATH 环境变量指定配置文件路径（默认 config.yaml）。config.yaml 不存在时不报错，自动降级到环境变量+默认值。applyEnvOverrides() 覆盖 18 个环境变量：SERVER_PORT/SERVER_HOST/DB_PATH/JWT_SECRET/JWT_EXPIRE_HOURS/LLM_API_KEY/LLM_BASE_URL/LLM_MODEL/LLM_EMBEDDING_MODEL/QDRANT_HOST/QDRANT_PORT/QDRANT_COLLECTION/QDRANT_VECTOR_SIZE/RAG_CHUNK_SIZE/RAG_CHUNK_OVERLAP/RAG_TOP_K。Validate() 检查 8 项：LLM_APIKey 非空、JWT.Secret 非空、Database.Path 非空、Server.Port 1-65535、LLM.BaseURL 非空、Qdrant.Host 非空、RAG.ChunkOverlap<ChunkSize、自动修正默认值(ExpireHours/VectorSize/ChunkSize/ChunkOverlap/TopK)。错误信息以列表形式展示每项含修复建议+环境变量名。PrintSummary() 格式化输出所有配置段，敏感值(APIKey/Secret)仅显示前4位+"***"，自动检测并列出已设置的环境变量名。main.go 使用 fmt.Fprintf(os.Stderr) 输出友好错误提示+os.Exit(1) 替代 log.Fatalf。所有数值型环境变量使用 strconv.Atoi 解析+范围校验，无效值静默忽略（保留 YAML/默认值）
- 知识图谱可视化重构 (Graph Redesign)：Graph.vue 从混乱的力导向布局重构为可切换的双模式可视化系统。布局模式：layoutMode ref（'hierarchical'/'force'）+ 模板 toggle 按钮组（SVG 图标+active 高亮），watch 切换时重建图表。层次布局算法 computeHierarchicalLayout：构建 outgoing/incoming 邻接表→找根节点（无入边优先，"核心概念"类别优先，全环图取最高度数节点）→BFS 分层（visited 防环，未连通节点放最后层）→重心排序（从第 2 层起按父节点平均 x 排序减少边交叉）→坐标计算（垂直间距 min(130,max(80))，水平间距 min(200,max(170))，居中排列）。使用 layout:'none'+ 显式 x,y 坐标。力导向模式增大 repulsion 2000+edgeLength [200,400] 适配大卡片，切换时 delete x/y 防止 ECharts 固定节点。卡片式节点 buildCardNode：160×56px roundRect，LinearGradient 左侧色条（category color 15%→白/深灰），ECharts rich text label（icon 14px category color Unicode 符号◆⊕∑▶★△▣◎⊞∫↔□+name 13px bold+desc 10px 灰色截断 16 字），shadowBlur 4+shadowOffsetY 2 悬浮感。categoryIcons 映射 14 类。边样式 buildEdgeConfig：源节点着色（nodeColorMap）、药丸标签 borderRadius 10、箭头缩小 6px、对比/关联虚线 [5,3]。共享 helper 消除 4 个样式函数 250 行重复代码：getCategoryColor（考虑跨材料模式）、buildCardNode（overrides: borderColor/borderWidth/shadowBlur/shadowColor/opacity+icon/name/desc color）、buildEdgeConfig（overrides: showLabel/edgeColor/edgeWidth/edgeOpacity/labelColor/labelFontSize/labelBg）。搜索/高亮/跨材料/右键菜单/PNG 导出/暗色模式全功能保留
- README 与项目文档完善 (T55)：README.md 全面重写为专业开源项目文档。结构：CI/Go/Vue/License 4 个 shields.io 徽章 → 项目简介段落 → 9 大功能特性模块（材料分析/卡片间隔重复/智能出题/AI对话RAG/知识图谱/学习路径/多Agent辩论/Dashboard成就/工程化安全）→ 技术架构表 11 行（含 KaTeX/Pinia 新增技术）→ 快速开始 5 步（含必需/可选配置表格+CONFIG_PATH/LLM_BASE_URL 等环境变量+Seed curl 示例）→ Docker 一键部署 → API 概览 8 大分类 40+ 端点表格（认证/材料/卡片/练习/错题本/对话/学习功能/图谱/其他）→ 完整项目结构目录树（含所有 T31-T54 新增文件：debate/pathplanner/mistake/notification/recommendation/learning_path/CardStudy/Debate/LearningPath/Mistakes/useMathRender 等）→ 截图占位 6 张（Dashboard/上传/卡片/对话/图谱/Quiz）→ 测试命令 → CI/CD 说明 → 配置参考链接（.env.example + config.yaml.example）→ 贡献指南含开发规范（user_id 隔离/useToast/dark:变体/响应式断点/路由注册/API client 6 条规范）→ MIT 许可证。README 中 GitHub OWNER 占位符需替换为实际用户名
- 学习路径加载修复 (OPT-13)：三个根因导致"总是加载不出来"——(1) LLM 常将 JSON 包裹在 ```json 代码块中，直接 json.Unmarshal 失败。extractJSON 辅助函数去除代码块围栏+修剪首尾非 JSON 文本（找第一个 {/[ 和最后一个 }/] 截断），已应用到全部 5 个 Agent（Analyst/CardMaker/QuizMaster/MapBuilder/PathPlanner）。(2) 后端无超时保护，context.WithTimeout 90s 防止 LLM API 挂起导致请求永久阻塞，超时返回 504 区分于普通 500 错误。(3) 前端 15s 超时过短（LLM 调用通常 30-90s），提升到 120s+axios timeout 120s。额外优化：30 分钟 per-user 内存缓存（pathCache map+sync.RWMutex），同用户 30 分钟内刷新页面直接返回缓存，避免每次访问都触发昂贵的 LLM 调用。"重新生成"按钮传 force=true 跳过缓存强制重新生成。骨架屏提示文案改为"通常需要 30-90 秒"管理用户预期
- 知识图谱思维导图布局 (Graph Mindmap)：Graph.vue 新增第三种布局模式——脑图（mindmap），从双模式扩展为三模式可视化系统。UI：模板 toggle 按钮组新增"脑图"按钮（径向太阳 SVG 图标+active 高亮），layoutMode ref 扩展为 'hierarchical'|'force'|'mindmap'，watch 切换时重建图表。径向布局算法 computeMindmapLayout（~100 行）：构建无向邻接表→选根节点（优先"核心概念"/"概念"类别，否则取最高度数节点）→BFS 树构造（visited 防环，断开节点挂到根节点 depth=1）→递归叶子计数（leafCount Map，叶子=1，非叶=子树叶子之和）→递归径向放置：根节点居中，子节点按叶子数比例分配角度扇区，半径逐层递增（radiusStep=min(width,height)*0.12，最小 80px），角度范围从 startAngle 到 startAngle+range，扇区中心角=(start+end)/2，递归展开子树。ECharts series layout 属性从条件判断改为 `layoutMode.value === 'force' ? 'force' : 'none'`，使 mindmap 和 hierarchical 均使用显式坐标（layout:'none'）。initChart 布局分发新增 mindmap 分支调用 computeMindmapLayout。卡片节点/边样式/搜索/高亮/跨材料/暗色模式等全部复用现有 helper 函数，零额外样式代码
